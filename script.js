const DATA = {
  brand: "辨证堂",
  clinic: {
    name: "小满医馆",
    level: "初入杏林",
    slogan: "不是刷题，而是坐诊",
    skill: "失眠辨证入门",
    scene: "今日有 3 位虚拟病人候诊，1 位病人正在模拟服药，1 位病人需要复诊调方",
    stats: [
      ["今日候诊", 3],
      ["正在服药", 1],
      ["待复诊", 1],
      ["累计病案", 5]
    ],
    reminder: {
      patient: "陈女士",
      status: "服药第2天",
      feedback: "腹胀加重，睡眠略有改善。",
      suggestion: "请判断是否需要调整方药。"
    }
  },
  patients: [
    {
      id: "lin",
      icon: "眠",
      name: "林女士",
      age: "32岁",
      identity: "设计师",
      complaint: "失眠半年，入睡困难",
      status: "最近压力大，经前烦躁明显，想知道为什么总睡不好。",
      focus: "情志失眠 / 肝郁与心神关系 / 方药加减",
      difficulty: "入门",
      tags: ["情志", "睡眠", "肝郁"],
      description: "最近半年一直睡不好，躺下很久才能睡着，心里烦，容易叹气，月经前乳房胀痛。",
      patientProfile: {
        name: "林女士",
        age: "32岁",
        identity: "设计师",
        avatar: "./assets/patients/lin-avatar.png",
        icon: "🌙",
        images: {
          initial: "./assets/patients/lin-insomnia-initial.png",
          day2: "./assets/patients/lin-insomnia-day2.png",
          recovered: "./assets/patients/lin-insomnia-recovered.png"
        },
        visualState: {
          initial: "坐在电脑前，黑眼圈明显，手扶额头，神情烦闷。",
          day1: "表情稍缓和，但仍有疲惫感。",
          day2: "坐在桌前喝热茶，眉头舒展一些。",
          recovered: "神情平和，准备合上电脑休息。"
        },
        today: "压力大，入睡困难，多梦，情绪烦闷，经前乳房胀痛。",
        result: "入睡明显改善，多梦减少，情绪较平稳。",
        badge: "情志初辨"
      },
      detailView: {
        chief: "林女士，32岁，这半年总觉得睡不踏实，躺下后要很久才能睡着，最近尤其明显。",
        observable: "患者神情有些疲惫，说话偏轻，眼下发青，情绪像一直绷着，没有完全放松下来。",
        inquiryOpener: "这半年我总觉得睡不实，躺下以后要磨很久才能睡着，最近工作一忙就更明显了。",
        entryStatus: {
          patient: "初诊中",
          task: "收集辨证证据",
          reminder: "不要急着下结论，先把病程、胸胁、月经、睡眠特点和舌脉线索问清楚。"
        },
        entryTips: {
          "病程长短": "病程能帮你判断这是短期波动，还是已经形成较稳定的病机。",
          "胸胁情况": "胸胁和乳房的胀满感，常能提示气机是不是郁住了。",
          "月经情况": "经前变化能帮助你判断睡眠问题是否和肝气疏泄失常有关。",
          "心悸健忘": "心悸健忘能帮助你判断有没有心脾不足这类兼证。",
          "饮食二便": "饮食和二便可以帮助判断脾胃是否明显参与其中。",
          "寒热汗出": "潮热盗汗等信息能帮助你判断热象是不是已经偏重。",
          "痰胸情况": "痰多胸闷如果突出，才更有必要把痰热这条线往前放。",
          "舌象": "舌苔能帮助判断寒热和痰湿，但不能脱离症状单独判断。",
          "脉象": "脉象是辅助证据，要和睡眠、情绪、胸胁和月经线索一起看。"
        },
        evidencePlan: [
          { label: "病程长短", locked: "未问" },
          { label: "胸胁情况", locked: "未问" },
          { label: "月经情况", locked: "未问" },
          { label: "心悸健忘", locked: "未问" },
          { label: "饮食二便", locked: "未问" },
          { label: "寒热汗出", locked: "未问" },
          { label: "痰胸情况", locked: "未问" },
          { label: "舌象", locked: "未看" },
          { label: "脉象", locked: "未切" }
        ]
      },
      basics: {
        职业: "设计师",
        情绪: "压力大，容易急躁",
        饮食: "食欲一般",
        睡眠: "入睡困难，多梦",
        二便: "大便基本正常",
        舌象: "舌淡红，苔薄白",
        脉象: "脉弦"
      },
      evidenceUnlocks: {
        病程: {
          category: "病程长短",
          finding: "病程半年",
          judge: {
            prompt: "这条信息更支持什么？",
            options: [
              { id: "A", label: "肝郁气滞" },
              { id: "B", label: "心脾两虚" },
              { id: "C", label: "阴虚火旺" },
              { id: "D", label: "暂时不能判断" }
            ],
            feedback: {
              A: "这个判断方向可以保留，但先别只靠病程下结论。病久说明问题不是一两天的波动，却还要和情绪、月经、舌脉放在一起看。",
              B: "这个方向可以先记着，但病程长本身还不够把它推到心脾两虚。单独一条病程，更像是在提醒你这不是急性波动。",
              C: "先别急着往阴虚火旺走。病久不等于有热，还要看口干、潮热、舌红少苔这些证据有没有跟上。",
              D: "这样想很稳。病程只能告诉你问题持续已久，还不能单独定证，后面还要继续看情绪、胸胁、月经和舌脉。 "
            }
          }
        },
        胸胁胀满: {
          category: "胸胁情况",
          finding: "经前胸胁胀满",
          judge: {
            prompt: "这条信息更支持什么？",
            options: [
              { id: "A", label: "肝郁气滞" },
              { id: "B", label: "心脾两虚" },
              { id: "C", label: "阴虚火旺" },
              { id: "D", label: "暂时不能判断" }
            ],
            feedback: {
              A: "这个判断方向对。这条证据很关键。胸胁、乳房这一带和肝的疏泄关系很近，经前更明显，常常是在提醒你气机不畅。",
              B: "这个方向先放一放。心脾两虚更常让人乏力、心悸、食少，这条“胸胁胀满”本身更像肝气不舒。",
              C: "还不够往阴虚火旺走。阴虚火旺更看潮热、口干、舌红，这条胸胁胀满更像气机郁着了。",
              D: "先不急着定证也可以，但这条证据已经在往肝郁方向推了。后面再把月经、情绪和脉象合起来看，会更清楚。"
            }
          }
        },
        月经: {
          category: "月经情况",
          finding: "经前乳胀，情绪波动",
          judge: {
            prompt: "这条信息更支持什么？",
            options: [
              { id: "A", label: "肝郁气滞" },
              { id: "B", label: "心脾两虚" },
              { id: "C", label: "阴虚火旺" },
              { id: "D", label: "暂时不能判断" }
            ],
            feedback: {
              A: "这个判断方向对。经前乳胀、情绪起伏常和肝气疏泄失常连在一起，是失眠案里很有辨识度的一组线索。",
              B: "先别急着往心脾两虚上落。月经前乳胀这条线，和气机郁滞的关系更近一些。",
              C: "这个方向还差证据。阴虚火旺更要看口干、潮热、舌红少苔，这里暂时没跟上。",
              D: "这样判断也可以，但这条信息已经很有方向感了。经前加重、乳胀明显，通常是在提醒你去看肝郁这条主线。"
            }
          }
        },
        心悸健忘: {
          category: "心悸健忘",
          finding: "偶有心慌，健忘不著",
          judge: {
            prompt: "这条信息更支持什么？",
            options: [
              { id: "A", label: "心脾两虚" },
              { id: "B", label: "肝郁气滞" },
              { id: "C", label: "阴虚火旺" },
              { id: "D", label: "暂时不能判断" }
            ],
            feedback: {
              A: "这个方向可以留作兼证考虑。偶有心慌会让人想到心脾不足，但这里只是轻度出现，还不能盖过肝郁主线。",
              B: "这个方向不能说错，但单看心慌还不够直接支撑肝郁。它更像是在提醒你后面别漏掉兼证。",
              C: "先别往阴虚火旺走。这里只是偶有心慌，并没有热扰心神那类更明显的热象。",
              D: "这样想很稳。偶有心慌说明还要留意兼证，但单独这一条还不能自己站成一个主证。"
            }
          }
        },
        纳差便溏: {
          category: "饮食二便",
          finding: "食欲一般，大便不稀",
          judge: {
            prompt: "这条信息更支持什么？",
            options: [
              { id: "A", label: "心脾两虚证据不足" },
              { id: "B", label: "痰热更明显" },
              { id: "C", label: "阴虚火旺更明显" },
              { id: "D", label: "暂时不能判断" }
            ],
            feedback: {
              A: "这个判断方向对。要是脾虚很明显，常会看到胃口差、便溏、乏力更突出；现在这条线并不重，所以心脾两虚证据还不够扎实。",
              B: "这里看不出痰热更明显。痰热更常见口苦、痰多、苔黄腻，这条信息没有把你往那边推。",
              C: "也还不够往阴虚火旺走。阴虚火旺更要看口干、潮热、舌红少苔，不是从这条饮食二便里出来的。",
              D: "这样想可以。这条信息的价值更多在于帮你排除，而不是单独证明哪一个证型。"
            }
          }
        },
        饮食: {
          category: "饮食二便",
          finding: "食欲一般，大便不稀",
          judge: {
            prompt: "这条信息更支持什么？",
            options: [
              { id: "A", label: "心脾两虚证据不足" },
              { id: "B", label: "痰热更明显" },
              { id: "C", label: "阴虚火旺更明显" },
              { id: "D", label: "暂时不能判断" }
            ],
            feedback: {
              A: "这个判断方向对。饮食和大便没有明显往下掉，说明脾虚不是现在最扎眼的那条线。",
              B: "还看不出痰热更明显。痰热一般不会只靠“食欲一般”来判断。",
              C: "也不足以把你推到阴虚火旺。热象证据还要从别处找。",
              D: "这样判断没问题。这条信息更像是在帮你排除，而不是直接定证。"
            }
          }
        },
        大便: {
          category: "饮食二便",
          finding: "食欲一般，大便不稀",
          judge: {
            prompt: "这条信息更支持什么？",
            options: [
              { id: "A", label: "心脾两虚证据不足" },
              { id: "B", label: "痰热更明显" },
              { id: "C", label: "阴虚火旺更明显" },
              { id: "D", label: "暂时不能判断" }
            ],
            feedback: {
              A: "这个判断方向对。大便不稀，让你先别急着把脾虚放太重。",
              B: "这里还看不出痰热。痰热会更依赖痰、口苦、苔黄腻这些线索。",
              C: "也还推不到阴虚火旺。单靠这条二便信息不够。",
              D: "可以，这条信息本来就是用来帮你校正方向的。"
            }
          }
        },
        潮热盗汗: {
          category: "寒热汗出",
          finding: "无明显潮热盗汗",
          judge: {
            prompt: "这条信息更支持什么？",
            options: [
              { id: "A", label: "阴虚火旺证据不足" },
              { id: "B", label: "肝郁气滞更明显" },
              { id: "C", label: "心脾两虚更明显" },
              { id: "D", label: "暂时不能判断" }
            ],
            feedback: {
              A: "这个判断方向对。没有明显潮热盗汗，就先别把阴虚火旺放到前面。",
              B: "这个方向可以作为整体判断保留，但这条证据本身更像是在帮你排除阴虚热象。",
              C: "还不能因为没有盗汗，就直接把方向推到心脾两虚。",
              D: "这样想可以。这类证据常常是排除题，不是单独定证题。"
            }
          }
        },
        痰多胸闷: {
          category: "痰胸情况",
          finding: "痰不多，偶有胸闷",
          judge: {
            prompt: "这条信息更支持什么？",
            options: [
              { id: "A", label: "痰热主线证据不足" },
              { id: "B", label: "肝郁气滞更明显" },
              { id: "C", label: "阴虚火旺更明显" },
              { id: "D", label: "暂时不能判断" }
            ],
            feedback: {
              A: "这个判断方向对。痰不多、胸闷也不算主诉，说明痰热扰心这条线证据还不够硬。",
              B: "这个方向可以保留，因为胸闷也可能和气机郁滞有关，但这条信息更主要是在帮你排除痰热主线。",
              C: "这里还推不到阴虚火旺。它和痰热、燥热都不够像。",
              D: "这样想也可以。单独一条“偶有胸闷”还站不起来，要和情绪、胸胁、月经一起看。"
            }
          }
        },
        舌象: {
          category: "舌象",
          finding: "舌淡红，苔薄白",
          judge: {
            prompt: "这条信息更支持什么？",
            options: [
              { id: "A", label: "热象不重" },
              { id: "B", label: "阴虚火旺更明显" },
              { id: "C", label: "痰热更明显" },
              { id: "D", label: "暂时不能判断" }
            ],
            feedback: {
              A: "这个判断方向对。苔薄白说明热势不重，也看不出痰热壅盛那种黏腻黄厚的样子。",
              B: "这条舌象还不支持阴虚火旺。真要往那边走，通常会更偏红、更少津。",
              C: "也不支持痰热更明显。要看到痰热，常常会期待苔黄腻一类的表现。",
              D: "这样想可以。舌象要和症状、脉象一起看，但这条至少在提醒你别过度往热证走。"
            }
          }
        },
        脉象: {
          category: "脉象",
          finding: "脉弦",
          judge: {
            prompt: "这条信息更支持什么？",
            options: [
              { id: "A", label: "肝郁气滞" },
              { id: "B", label: "心脾两虚" },
              { id: "C", label: "阴虚火旺" },
              { id: "D", label: "暂时不能判断" }
            ],
            feedback: {
              A: "这个判断方向对。脉弦常常让人想到肝郁、气机不舒，这和她的情绪波动、胸胁不舒能接起来。",
              B: "脉弦并不是心脾两虚最典型的脉象，所以这条证据不能把主线放到那边。",
              C: "也还不够往阴虚火旺走。阴虚火旺更常看舌红少苔、心烦口干一类的热象搭配。",
              D: "可以先保留，但这条脉象方向性其实已经不弱了。等你把胸胁、月经线索也拼上，会更像肝郁主线。"
            }
          }
        }
      },
      hintLadders: [
        {
          id: "lin-duration",
          title: "病程长短",
          key: "病程",
          actionQuestion: "这种情况大概持续多久了？",
          stages: [
            "你还缺少一个判断病情轻重和演变方向的时间线索。",
            "失眠类问题先看病程长短，能帮你分清是短期波动，还是已经形成稳定病机。",
            "可以追问：这种情况大概持续多久了？最近是一直这样，还是时轻时重？"
          ]
        },
        {
          id: "lin-rib",
          title: "胸胁情况",
          key: "胸胁胀满",
          actionQuestion: "经前会不会觉得胸胁胀满，或者乳房发胀？",
          stages: [
            "你还缺少一个判断气机是否郁滞的重要线索。",
            "情志相关失眠，常要追问胸胁和乳房有没有经前胀满、不舒。",
            "可以追问：经前会不会觉得胸胁胀满，或者乳房发胀？"
          ]
        },
        {
          id: "lin-period",
          title: "月经情况",
          key: "月经",
          actionQuestion: "月经前后，睡眠和情绪会不会有变化？",
          stages: [
            "你还缺少一个和肝郁相关度很高的女性线索。",
            "经前情绪、乳房和睡眠的变化，常能帮你看出是不是肝气在牵动心神。",
            "可以追问：月经前后，睡眠和情绪会不会有变化？"
          ]
        },
        {
          id: "lin-heart",
          title: "心悸健忘",
          key: "心悸健忘",
          actionQuestion: "平时会不会心慌，或者记性明显变差？",
          stages: [
            "你还缺少一个判断有没有兼证的重要线索。",
            "心慌、健忘这些表现，能帮助你分辨是不是还有心脾不足一类的问题。",
            "可以追问：平时会不会心慌，或者记性明显变差？"
          ]
        },
        {
          id: "lin-digest",
          title: "饮食二便",
          key: "纳差便溏",
          actionQuestion: "最近胃口和大便怎么样，有没有食欲差或便溏？",
          stages: [
            "你还缺少一个判断脾胃是否明显参与的线索。",
            "饮食和二便，能帮助你判断要不要把心脾两虚放得更靠前。",
            "可以追问：最近胃口和大便怎么样，有没有食欲差或便溏？"
          ]
        },
        {
          id: "lin-heat",
          title: "寒热汗出",
          key: "潮热盗汗",
          actionQuestion: "最近有没有潮热、盗汗，或者夜里出汗的情况？",
          stages: [
            "你还缺少一个判断热象强弱的线索。",
            "潮热、盗汗这类信息，能帮你判断阴虚火旺是不是一条需要认真考虑的路。",
            "可以追问：最近有没有潮热、盗汗，或者夜里出汗的情况？"
          ]
        },
        {
          id: "lin-phlegm",
          title: "痰胸情况",
          key: "痰多胸闷",
          actionQuestion: "平时会不会痰多、胸闷，或者觉得胸口堵得慌？",
          stages: [
            "你还缺少一个判断痰热扰心是否成立的线索。",
            "如果痰多、胸闷更突出，才更有必要把痰热这条线往前放。",
            "可以追问：平时会不会痰多、胸闷，或者觉得胸口堵得慌？"
          ]
        },
        {
          id: "lin-tongue",
          title: "舌象",
          key: "舌象",
          actionQuestion: "我想补看一下舌象。",
          actionLabel: "补看舌象",
          stages: [
            "你还缺少一个帮助判断寒热与痰湿的观察证据。",
            "舌苔的颜色和厚薄，能帮你判断热象重不重、痰湿显不显。",
            "可以补看舌象：看看舌质偏淡还是偏红，舌苔是薄白、黄还是腻。"
          ]
        },
        {
          id: "lin-pulse",
          title: "脉象",
          key: "脉象",
          actionQuestion: "我想补切一下脉象。",
          actionLabel: "补切脉象",
          stages: [
            "你还缺少一个帮助确认病机走向的体征证据。",
            "脉象常常能帮你把“气机郁滞、虚弱不足、热扰心神”这些方向再分开一点。",
            "可以补切脉象：留意脉是偏弦、偏细，还是数而有热势。"
          ]
        }
      ]
    },
    {
      id: "zhou",
      icon: "寒",
      name: "周同学",
      age: "21岁",
      identity: "大学生",
      complaint: "鼻塞、喷嚏、头痛 1 天",
      status: "明天有考试，今天鼻塞、喷嚏、头痛，状态焦急。",
      focus: "普通感冒辨别，方药与中成药选择",
      difficulty: "入门",
      tags: ["感冒", "入门", "方药"],
      description: "我明天还有考试，结果今天一直打喷嚏、流清鼻涕，头也痛，身上酸，特别怕冷。昨天晚上空调开得有点低，还吹了风。体温量了一下 37.5℃，嗓子有点不舒服，但不是特别疼。",
      patientProfile: {
        name: "周同学",
        age: "21岁",
        identity: "大学生",
        avatar: "./assets/patients/zhou-avatar.png",
        icon: "🤧",
        images: {
          initial: "./assets/patients/zhou-cold-initial.png",
          day2: "./assets/patients/zhou-cold-day2.png",
          recovered: "./assets/patients/zhou-cold-recovered.png"
        },
        visualState: {
          initial: "裹着外套，拿着纸巾，鼻尖微红，表情有点焦急。",
          day1: "裹着外套，手里还拿着纸巾，表情稍缓和。",
          day2: "外套稍微敞开，纸巾减少，表情比初诊轻松。",
          recovered: "精神恢复，坐在书桌前，状态轻松。"
        },
        today: "怕冷明显，流清涕，头痛身痛，明天有考试有点着急。",
        result: "鼻塞基本缓解，身痛明显减轻，低热消退。",
        badge: "风寒初辨"
      },
      detailView: {
        chief: "周同学，21岁，明天有考试，今天一直打喷嚏、流鼻涕、头痛，觉得特别怕冷。",
        observable: "患者裹着外套，声音有鼻音，精神略疲惫，偶尔咳嗽，说话时会下意识吸鼻子。",
        inquiryOpener: "我明天还有考试，今天一直打喷嚏、流鼻涕，头也痛，整个人发紧，还挺怕冷的。",
        entryStatus: {
          patient: "初诊中",
          task: "收集辨证证据",
          reminder: "不要急着选方，先把寒热、汗出、痰涕、舌脉问清楚。"
        },
        entryTips: {
          "发病环境": "发病环境能帮助你判断外邪是怎么进来的，是受凉受风，还是闷热夹湿。",
          "寒热轻重": "先判断怕冷更重，还是发热更重，这是区分风寒、风热的重要入口。",
          "汗出情况": "汗出可以帮助判断表邪是闭住了，还是已经有外泄趋势。",
          "鼻涕性质": "鼻涕清稀多偏寒，黄稠多偏热，但要结合其他症状判断。",
          "痰的性质": "痰白清稀多偏寒，痰黄黏稠多偏热。",
          "咽喉情况": "咽痛明显常提示热象，但轻微咽不适不能直接判断为风热。",
          "舌象": "舌象可以辅助判断寒热、津液和湿邪，但不能脱离症状单独定证。",
          "脉象": "脉象是辅助证据，要和寒热、汗出、痰涕一起看。"
        },
        evidencePlan: [
          { label: "寒热轻重", locked: "未问" },
          { label: "汗出情况", locked: "未问" },
          { label: "鼻涕性质", locked: "未问" },
          { label: "痰的性质", locked: "未问" },
          { label: "咽喉情况", locked: "未问" },
          { label: "舌象", locked: "未看" },
          { label: "脉象", locked: "未切" }
        ]
      },
      basics: {
        "职业/身份": "大学生",
        起病诱因: "空调低温、吹风后发作",
        寒热: "怕冷明显，发热轻",
        汗出: "无明显出汗",
        鼻涕: "清稀",
        咳痰: "少量白稀痰",
        咽喉: "轻微不适，不明显红肿疼痛",
        舌象: "舌淡红，苔薄白",
        脉象: "脉浮紧"
      },
      masteredInfo: ["起病诱因", "怕冷", "鼻涕", "咳嗽", "舌象", "脉象"],
      pendingInfo: ["汗出", "咽痛", "痰色", "胃肠症状", "口渴"],
      questions: [
        { key: "汗", question: "有没有出汗？" },
        { key: "鼻涕", question: "鼻涕是清稀还是黄稠？" },
        { key: "咽痛", question: "咽喉是否红肿疼痛？" },
        { key: "痰", question: "痰是白稀还是黄稠？" },
        { key: "怕冷", question: "怕冷明显还是发热明显？" },
        { key: "口渴", question: "是否口渴想喝冷水？" },
        { key: "胃肠", question: "是否有腹胀、恶心、腹泻？" }
      ],
      knowledgeBase: [
        { key: "怕冷", aliases: ["怕冷", "恶寒", "发热明显", "怕冷明显"], answer: "怕冷比较明显，比发热更明显，盖上被子会舒服些。", relevance: "high" },
        { key: "发热", aliases: ["发热", "体温", "高热", "低热"], answer: "体温 37.5℃，有点低热，但没有明显高热。", relevance: "medium" },
        { key: "汗", aliases: ["汗", "出汗", "汗出", "无汗", "发汗", "有汗"], answer: "没有明显出汗。", relevance: "high" },
        { key: "鼻涕", aliases: ["鼻涕", "流涕", "黄稠", "清稀"], answer: "鼻涕是清稀的，不黄不稠。", relevance: "high" },
        { key: "清涕", aliases: ["清涕", "清鼻涕"], answer: "鼻涕清稀，像水一样。", relevance: "high" },
        { key: "咽喉", aliases: ["咽喉", "嗓子", "发痒", "红肿"], answer: "嗓子有点不舒服，更像发痒，不是明显红肿疼痛。", relevance: "medium" },
        { key: "咽痛", aliases: ["咽痛", "喉咙痛", "疼痛"], answer: "不明显咽痛，也没有明显红肿。", relevance: "medium" },
        { key: "痰", aliases: ["痰", "痰色", "咳痰", "白痰", "黄痰"], answer: "痰不多，是白色偏稀的痰。", relevance: "high" },
        { key: "头痛", aliases: ["头痛", "后脑", "项背"], answer: "头有点痛，后脑和项背部有点紧。", relevance: "medium" },
        { key: "身痛", aliases: ["身痛", "酸痛", "身上酸", "头痛身痛"], answer: "身上酸痛明显，像受凉后那种酸痛。", relevance: "medium" },
        { key: "口渴", aliases: ["口渴", "冷水", "想喝水"], answer: "不怎么口渴，也不特别想喝冷水。", relevance: "medium" },
        { key: "胃肠", aliases: ["胃肠", "腹胀", "恶心", "腹泻"], answer: "没有明显腹胀、恶心、腹泻。", relevance: "medium" },
        { key: "腹泻", aliases: ["腹泻", "拉肚子"], answer: "没有腹泻。", relevance: "medium" },
        { key: "空调", aliases: ["空调", "低温"], answer: "昨晚空调开得比较低，还吹了风，之后开始不舒服。", relevance: "high" },
        { key: "吹风", aliases: ["吹风", "受风"], answer: "昨晚吹风后开始鼻塞、喷嚏、怕冷。", relevance: "high" }
      ],
      evidenceUnlocks: {
        空调: {
          category: "发病环境",
          finding: "受凉吹风后发作",
          countInProgress: false,
          judge: {
            prompt: "这条信息更支持什么？",
            options: [
              { id: "A", label: "外感风寒线索更强" },
              { id: "B", label: "风热线索更强" },
              { id: "C", label: "暑湿线索更强" },
              { id: "D", label: "暂时不能判断" }
            ],
            feedback: {
              A: "这个判断方向对。这条线索很关键。受凉、吹风、空调低温以后马上不舒服，更像外寒从体表进来，先把风寒这条路放到前面。",
              B: "先别往风热走。风热更常看发热重、咽痛、口渴、黄涕，这条“受凉后发作”并不在推它。",
              C: "也还不支持暑湿。暑湿更依赖闷热环境、胃肠不适、身重困倦这些线索。",
              D: "这样想可以，但这条发病环境已经在给你方向了。外感案先看怎么起病，受凉之后马上发作，通常更像风寒。"
            }
          }
        },
        吹风: {
          category: "发病环境",
          finding: "受凉吹风后发作",
          countInProgress: false,
          judge: {
            prompt: "这条信息更支持什么？",
            options: [
              { id: "A", label: "外感风寒线索更强" },
              { id: "B", label: "风热线索更强" },
              { id: "C", label: "暑湿线索更强" },
              { id: "D", label: "暂时不能判断" }
            ],
            feedback: {
              A: "这个判断方向对。吹风受凉后马上发作，很像外寒袭表的开端。",
              B: "这里还推不到风热。风热更看热象，不是先看吹风受凉。",
              C: "也还不是暑湿主线。这里缺少闷热、困重、胃肠不适那类线索。",
              D: "可以先保留，但这条环境线索本身已经在往风寒方向推。"
            }
          }
        },
        汗: {
          category: "汗出情况",
          finding: "无明显出汗",
          countInProgress: true,
          judge: {
            prompt: "这条信息更支持什么？",
            options: [
              { id: "A", label: "风寒束表" },
              { id: "B", label: "风热袭表" },
              { id: "C", label: "暑湿困表" },
              { id: "D", label: "暂时不能判断" }
            ],
            feedback: {
              A: "这个判断方向对。这条证据很关键。无汗像皮肤的“门”被关住了，身体想发散却发不出来，所以“怕冷明显 + 无汗”更支持风寒。",
              B: "这个方向先放一放。风热更常见微汗、咽痛、口渴、黄涕，这条“无汗”并没有把你往那里推。",
              C: "也还不够往暑湿走。暑湿常会有闷、重、胃肠不适，不是只靠这一条无汗来判断。",
              D: "先不急着定证也可以，但这条信息已经很有方向感了。无汗本身就在提醒你去看表闭、看风寒。"
            }
          }
        },
        鼻涕: {
          category: "鼻涕性质",
          finding: "鼻涕清稀",
          countInProgress: true,
          judge: {
            prompt: "这条信息更支持什么？",
            options: [
              { id: "A", label: "风寒束表" },
              { id: "B", label: "风热袭表" },
              { id: "C", label: "暑湿困表" },
              { id: "D", label: "暂时不能判断" }
            ],
            feedback: {
              A: "这个判断方向对。清涕偏寒，像身体还没把寒邪化热，常和风寒表证站在一边。",
              B: "这里不太支持风热。风热更常见黄涕、咽痛、口渴，清涕正好和它拉开了距离。",
              C: "也还不是暑湿的味道。暑湿更常把重点落在身重、胃肠和环境闷热上。",
              D: "这样想可以，但这条鼻涕性质其实很有用。外感案里，清还是黄，常常就是寒和热的分岔口。"
            }
          }
        },
        清涕: {
          category: "鼻涕性质",
          finding: "鼻涕清稀",
          countInProgress: true,
          judge: {
            prompt: "这条信息更支持什么？",
            options: [
              { id: "A", label: "风寒束表" },
              { id: "B", label: "风热袭表" },
              { id: "C", label: "暑湿困表" },
              { id: "D", label: "暂时不能判断" }
            ],
            feedback: {
              A: "这个判断方向对。清涕往往比“流鼻涕”更有辨别力，它更偏寒象。",
              B: "这里先别往风热上落。风热一般更期待黄涕、咽痛、口渴。",
              C: "也还看不出暑湿。暑湿不会只靠清涕来定。",
              D: "先保留也可以，但这条信息其实已经在把你往寒象那边推了。"
            }
          }
        },
        痰: {
          category: "痰的性质",
          finding: "少量白稀痰",
          countInProgress: true,
          judge: {
            prompt: "这条信息更支持什么？",
            options: [
              { id: "A", label: "风寒束表" },
              { id: "B", label: "风热袭表" },
              { id: "C", label: "痰热主线" },
              { id: "D", label: "暂时不能判断" }
            ],
            feedback: {
              A: "这个判断方向对。痰白而稀，常常还是寒象，没有化成热黏住。",
              B: "这里不支持风热。风热咳痰更常见偏黄、偏黏，喉咙也会更痛。",
              C: "也还到不了痰热。痰热讲的是热把痰熬稠了，不是现在这种白而稀。",
              D: "可以先保留，但这条痰色信息其实很有分量。白稀痰常常是在帮你把热证往后排。"
            }
          }
        },
        咽喉: {
          category: "咽喉情况",
          finding: "咽部轻微不适，无明显红肿疼痛",
          countInProgress: true,
          judge: {
            prompt: "这条信息更支持什么？",
            options: [
              { id: "A", label: "风热证据不足" },
              { id: "B", label: "风热更明显" },
              { id: "C", label: "暑湿更明显" },
              { id: "D", label: "暂时不能判断" }
            ],
            feedback: {
              A: "这个判断方向对。要是风热更明显，咽喉常会更红、更痛、更灼，这里只是轻微不适，热象还不够硬。",
              B: "这里先别急着往风热走。轻微不适不等于明显咽痛，方向感还不一样。",
              C: "也还和暑湿不搭。暑湿更常让人闷、重、胃口差，而不是单靠这条咽喉信息。",
              D: "这样想可以。这条证据主要在提醒你：不要因为‘嗓子不舒服’四个字，就太快把它当成风热。"
            }
          }
        },
        咽痛: {
          category: "咽喉情况",
          finding: "咽部轻微不适，无明显红肿疼痛",
          countInProgress: true,
          judge: {
            prompt: "这条信息更支持什么？",
            options: [
              { id: "A", label: "风热证据不足" },
              { id: "B", label: "风热更明显" },
              { id: "C", label: "暑湿更明显" },
              { id: "D", label: "暂时不能判断" }
            ],
            feedback: {
              A: "这个判断方向对。没有明显红肿热痛，就先别把风热放到最前面。",
              B: "这个方向还差证据。风热的咽痛通常更鲜明，不是这种轻轻带过。",
              C: "也不支持暑湿。它的重点不在这里。",
              D: "可以，这条信息更像是在帮你排除，而不是单独定证。"
            }
          }
        },
        舌象: {
          category: "舌象",
          finding: "舌淡红，苔薄白",
          countInProgress: true,
          judge: {
            prompt: "这条信息更支持什么？",
            options: [
              { id: "A", label: "风寒束表" },
              { id: "B", label: "风热袭表" },
              { id: "C", label: "暑湿困表" },
              { id: "D", label: "暂时不能判断" }
            ],
            feedback: {
              A: "这个判断方向对。苔薄白说明热势不重，也没有湿热那种黄腻黏滞，更像表寒初起。",
              B: "这里还不支持风热。真往风热走，舌苔往往更容易偏黄。",
              C: "也还不是暑湿。暑湿更常见腻苔、身重、胃肠不适这些组合。",
              D: "这样想也可以，但这条舌象已经在帮你把热和湿往后排了。"
            }
          }
        },
        脉象: {
          category: "脉象",
          finding: "脉浮紧",
          countInProgress: true,
          judge: {
            prompt: "这条信息更支持什么？",
            options: [
              { id: "A", label: "风寒束表" },
              { id: "B", label: "风热袭表" },
              { id: "C", label: "暑湿困表" },
              { id: "D", label: "暂时不能判断" }
            ],
            feedback: {
              A: "这个判断方向对。这条证据很关键。脉浮说明邪在表，脉紧又像被寒收住，所以合起来更像风寒束表。",
              B: "这里先别往风热走。风热更常期待脉浮数，而不是浮紧。",
              C: "也不太像暑湿。暑湿更常见濡、缓、困重，不是这种被寒收住的感觉。",
              D: "先保留也可以，但这条脉象其实很有指向性。浮在表、紧在寒，组合起来就很像风寒。"
            }
          }
        },
        怕冷: {
          category: "寒热轻重",
          finding: "恶寒明显，发热较轻",
          countInProgress: false,
          judge: {
            prompt: "这条信息更支持什么？",
            options: [
              { id: "A", label: "风寒束表" },
              { id: "B", label: "风热袭表" },
              { id: "C", label: "暑湿困表" },
              { id: "D", label: "暂时不能判断" }
            ],
            feedback: {
              A: "这个判断方向对。恶寒重、发热轻，常常是在提醒你寒邪还压在表面。",
              B: "先别往风热走。风热一般更容易看到发热偏重、咽痛口渴跟着来。",
              C: "也还没有把你推到暑湿。暑湿更讲闷、重、困，不是单靠恶寒轻重。",
              D: "可以先保留，但这条寒热轻重其实已经在往风寒那边倾斜了。"
            }
          }
        },
        发热: {
          category: "寒热轻重",
          finding: "恶寒明显，发热较轻",
          countInProgress: false,
          judge: {
            prompt: "这条信息更支持什么？",
            options: [
              { id: "A", label: "风寒束表" },
              { id: "B", label: "风热袭表" },
              { id: "C", label: "暑湿困表" },
              { id: "D", label: "暂时不能判断" }
            ],
            feedback: {
              A: "这个判断方向对。低热轻、恶寒重，通常比“高热明显”更像风寒。",
              B: "这里还不够支撑风热。风热多是热象更往前站。",
              C: "也还看不出暑湿。暑湿不会只靠轻低热来判断。",
              D: "这样想也可以，但这条信息至少在提醒你：热不占上风。"
            }
          }
        }
      },
      hintLadders: [
        {
          id: "cold-environment",
          title: "发病环境",
          key: "空调",
          actionQuestion: "昨天有没有受凉、吹风，或者空调开得比较低？",
          stages: [
            "你还缺少一个判断外邪从哪里来的线索。",
            "外感感冒先要看受凉、吹风、空调、天气变化这类诱因。",
            "可以追问：昨天有没有受凉、吹风，或者空调开得比较低？"
          ]
        },
        {
          id: "cold-coldheat",
          title: "寒热轻重",
          key: "怕冷",
          actionQuestion: "怕冷明显还是发热明显？",
          stages: [
            "你还缺少一个帮助区分寒热偏向的重要线索。",
            "外感初起时，恶寒重还是发热重，往往能先把方向分开。",
            "可以追问：怕冷明显还是发热明显？"
          ]
        },
        {
          id: "cold-sweat",
          title: "汗出情况",
          key: "汗",
          actionQuestion: "有没有出汗？出汗后会不会舒服一些？",
          stages: [
            "你还缺少一个判断表证开合的重要信息。",
            "表证开合，常常要看有没有汗、出汗后舒服不舒服。",
            "可以追问：有没有出汗？出汗后会不会舒服一些？"
          ]
        },
        {
          id: "cold-nose",
          title: "鼻涕性质",
          key: "鼻涕",
          actionQuestion: "鼻涕是清稀的，还是发黄发稠的？",
          stages: [
            "你还缺少一个帮助判断寒热偏向的鼻部线索。",
            "鼻涕偏清还是偏黄，常常能帮你区分是偏寒还是偏热。",
            "可以追问：鼻涕是清稀的，还是发黄发稠的？"
          ]
        },
        {
          id: "cold-phlegm",
          title: "痰的颜色和稀稠",
          key: "痰",
          actionQuestion: "咳出来的痰是白稀的，还是黄稠的？",
          stages: [
            "你还缺少一个判断肺里偏寒还是偏热的线索。",
            "痰白而稀，和痰黄而黏，方向感差别很大。",
            "可以追问：咳出来的痰是白稀的，还是黄稠的？"
          ]
        },
        {
          id: "cold-throat",
          title: "咽喉情况",
          key: "咽痛",
          actionQuestion: "咽喉是明显红肿疼痛，还是只是发痒、不太舒服？",
          stages: [
            "你还缺少一个帮助排除风热的重要线索。",
            "如果是风热，咽喉常会更红、更痛；如果只是轻微不适，方向就不一样。",
            "可以追问：咽喉是明显红肿疼痛，还是只是发痒、不太舒服？"
          ]
        },
        {
          id: "cold-tongue",
          title: "舌象",
          key: "舌象",
          actionQuestion: "我想补看一下舌象。",
          actionLabel: "补看舌象",
          stages: [
            "你还缺少一个帮助判断寒热轻重的观察证据。",
            "舌苔颜色和厚薄，能帮助你看出外邪偏寒还是偏热。",
            "可以补看舌象：看看舌质和舌苔是偏白、偏黄，还是有腻感。"
          ]
        },
        {
          id: "cold-pulse",
          title: "脉象",
          key: "脉象",
          actionQuestion: "我想补切一下脉象。",
          actionLabel: "补切脉象",
          stages: [
            "你还缺少一个帮助确认表寒表热的体征证据。",
            "脉是浮紧还是浮数，常常能把风寒和风热再分开一步。",
            "可以补切脉象：留意脉象更偏浮紧还是浮数。"
          ]
        }
      ]
    },
    {
      id: "wang",
      icon: "胃",
      name: "王先生",
      age: "45岁",
      identity: "销售经理",
      complaint: "胃胀反酸，饭后明显",
      status: "工作应酬多，饮食不规律，最近饭后胃胀、反酸明显，常觉得胸脘堵闷。",
      focus: "脾胃气机 / 饮食内伤 / 胃脘不适辨证",
      difficulty: "进阶",
      tags: ["脾胃", "气机", "痰湿"]
      ,
      patientProfile: {
        name: "王先生",
        age: "45岁",
        identity: "销售经理",
        avatar: "./assets/patients/wang-avatar.png",
        icon: "🍵",
        images: {
          initial: "./assets/patients/wang-stomach-initial.png",
          day2: "./assets/patients/wang-stomach-day2.png",
          recovered: "./assets/patients/wang-stomach-recovered.png"
        },
        visualState: {
          initial: "饭后轻捂上腹，表情胀闷，有反酸不适感。",
          day2: "坐姿放松一些，饭后胀感减轻，但仍有轻微不适。",
          recovered: "面色平和，饮食清淡，状态稳定。"
        },
        today: "工作应酬多，饮食不规律，饭后胃胀反酸，胸脘堵闷。",
        result: "胃胀反酸减轻，饮食状态较稳定。",
        badge: "脾胃观察"
      }
    }
  ],
  masteredInfo: ["主诉", "情绪", "睡眠", "舌象", "脉象"],
  pendingInfo: ["病程", "寒热", "胸胁", "月经", "心悸", "饮食二便", "潮热盗汗", "痰多胸闷"],
  questions: [
    { key: "口苦咽干", question: "是否口苦咽干？" },
    { key: "胸胁胀满", question: "是否胸胁胀满？" },
    { key: "心悸健忘", question: "是否心悸健忘？" },
    { key: "月经", question: "月经情况如何？" },
    { key: "纳差便溏", question: "是否纳差便溏？" },
    { key: "潮热盗汗", question: "是否潮热盗汗？" },
    { key: "痰多胸闷", question: "是否痰多胸闷？" }
  ],
  patientKnowledgeBase: [
    { key: "病程", aliases: ["病程", "多久", "多长时间", "几天", "几个月", "几年", "什么时候开始", "持续多久", "这种情况多久"], answer: "这种情况大概有半年了，最近压力大的时候更明显。", relevance: "high" },
    { key: "口苦咽干", aliases: ["口苦咽干", "口苦", "咽干", "口干"], answer: "偶尔口干，不明显口苦。", relevance: "medium" },
    { key: "胸胁胀满", aliases: ["胸胁胀满", "胸胁", "胁胀", "胀满"], answer: "经前胸胁胀满明显，乳房胀痛也会加重。", relevance: "high" },
    { key: "心悸健忘", aliases: ["心悸健忘", "心悸", "心慌", "健忘"], answer: "偶尔心慌，但不明显健忘。", relevance: "medium" },
    { key: "月经", aliases: ["月经", "经前", "经量", "经期"], answer: "经前情绪波动大，乳房胀痛，经量基本正常。", relevance: "high" },
    { key: "纳差便溏", aliases: ["纳差便溏", "纳差", "便溏"], answer: "食欲一般，大便不稀。", relevance: "medium" },
    { key: "饮食", aliases: ["饮食", "食欲", "胃口", "吃饭"], answer: "食欲一般，最近没有明显变化。", relevance: "medium" },
    { key: "大便", aliases: ["大便", "便秘", "腹泻"], answer: "大便基本正常，不稀，也不明显便秘。", relevance: "medium" },
    { key: "潮热盗汗", aliases: ["潮热盗汗", "潮热", "盗汗", "出汗"], answer: "没有明显潮热盗汗。", relevance: "medium" },
    { key: "痰多胸闷", aliases: ["痰多胸闷", "痰多", "胸闷", "痰"], answer: "没有明显痰多，偶尔胸闷，更多是在情绪紧张时出现。", relevance: "medium" },
    { key: "怕冷怕热", aliases: ["怕冷怕热", "怕冷", "怕热", "寒热"], answer: "没有明显怕冷，也没有明显怕热。", relevance: "low-medium" },
    { key: "小便", aliases: ["小便", "尿"], answer: "小便基本正常。", relevance: "low" },
    { key: "睡眠", aliases: ["睡眠", "失眠", "入睡", "多梦", "夜醒"], answer: "主要是入睡困难，多梦，夜间容易醒。", relevance: "medium" },
    { key: "情绪", aliases: ["情绪", "压力", "急躁", "叹气", "烦躁"], answer: "最近压力比较大，容易急躁，也容易叹气。", relevance: "medium" },
    { key: "乳房胀痛", aliases: ["乳房胀痛", "乳胀", "乳房"], answer: "月经前乳房胀痛比较明显。", relevance: "high" }
  ],
  diagnosis: {
    八纲判断: { multi: true, options: ["寒", "热", "虚", "实", "表", "里"], selected: ["里", "实"] },
    病位判断: { multi: true, options: ["肝", "心", "脾", "肾", "胆"], selected: ["肝", "心"] },
    主证判断: { options: ["肝郁气滞", "心脾两虚", "阴虚火旺", "痰热扰心", "心胆气虚"], selected: ["肝郁气滞"] },
    治法选择: { options: ["疏肝解郁，养血安神", "补益心脾，养血安神", "滋阴降火，清心安神", "清热化痰，和中安神", "益气镇惊，安神定志"], selected: ["疏肝解郁，养血安神"] },
    参考方选择: { options: ["逍遥散合酸枣仁汤加减", "归脾汤加减", "黄连阿胶汤加减", "温胆汤加减", "安神定志丸加减"], selected: ["逍遥散合酸枣仁汤加减"] }
  },
  coldDiagnosis: {
    证型判断: { options: ["风寒感冒", "风热感冒", "暑湿感冒"], selected: [] },
    治法选择: { options: ["辛温解表，宣肺散寒", "辛凉解表，清宣肺热", "清暑化湿，解表和中", "滋阴清热，润肺止咳"], selected: [] },
    方剂选择: { options: ["荆防败毒散加减", "麻黄汤加减", "桂枝汤加减", "银翘散加减", "藿香正气散加减"], selected: [] },
    中药配方选择: {
      multi: true,
      options: ["荆芥", "金银花", "防风", "连翘", "羌活", "薄荷", "独活", "牛蒡子", "柴胡", "黄芩", "前胡", "石膏", "桔梗", "香薷", "枳壳", "藿香", "茯苓", "佩兰", "川芎", "麦冬", "甘草", "沙参", "生姜"],
      selected: []
    },
    中成药选择: {
      multi: true,
      options: ["风寒感冒颗粒", "荆防颗粒", "感冒清热颗粒", "银翘解毒丸", "桑菊感冒片", "藿香正气水", "双黄连口服液", "连花清瘟胶囊"],
      selected: []
    }
  },
  coldAnswer: {
    syndrome: "风寒感冒",
    method: "辛温解表，宣肺散寒",
    formula: "荆防败毒散加减",
    formulaGraph: {
      "荆防败毒散加减": { syndrome: "风寒", relation: "本案首选", score: 92, note: "图谱中由风寒节点强连接到荆防败毒散。本案恶寒重、无汗、清涕、身痛、苔薄白、脉浮紧，方向最匹配。" },
      "麻黄汤加减": { syndrome: "风寒", relation: "相关但需辨别", score: 78, note: "麻黄汤也属于风寒表实方向，和无汗、恶寒、身痛有关；但本案是普通感冒训练，鼻塞喷嚏、咳嗽清涕更适合先用荆防败毒散方向。" },
      "桂枝汤加减": { syndrome: "风寒", relation: "相关但证据不足", score: 66, note: "桂枝汤偏风寒表虚、有汗、营卫不和。本案明确无汗，因此与图谱风寒节点有关，但不是本案主线。" },
      "银翘散加减": { syndrome: "风热", relation: "证型偏离", score: 58, note: "银翘散经风热节点连接，需发热重、咽痛、口渴、黄涕黄痰等证据。本案这些证据不足。" },
      "藿香正气散加减": { syndrome: "暑湿", relation: "证型偏离", score: 60, note: "藿香正气散偏暑湿困中方向，更看胸闷、泛恶、腹胀便溏、身重困倦等证据。本案不符合。" },
      "桑菊饮加减": { syndrome: "风热", relation: "证型偏离", score: 56, note: "桑菊饮偏风热咳嗽方向。本案痰白清稀、恶寒明显，更支持风寒。" },
      "新加香薷饮加减": { syndrome: "暑湿", relation: "证型偏离", score: 60, note: "新加香薷饮经暑湿节点连接，需暑湿环境、身热不扬、胃肠湿阻等证据。本案胃肠症状不突出。" },
      "白虎加人参汤": { syndrome: "热盛/气津两伤", relation: "不匹配", score: 45, note: "白虎加人参汤偏气分热盛、气津两伤方向，需高热、汗出、口渴、脉洪大等证据。本案不符合。" }
    },
    herbs: ["荆芥", "防风", "羌活", "独活", "柴胡", "前胡", "桔梗", "枳壳", "茯苓", "川芎", "甘草", "生姜"],
    herbFunctions: {
      荆芥: "疏风解表",
      防风: "祛风解表",
      羌活: "散寒止痛",
      独活: "祛风除湿",
      柴胡: "疏散退热",
      前胡: "宣肺化痰",
      桔梗: "宣肺利咽",
      枳壳: "理气宽中",
      茯苓: "健脾渗湿",
      川芎: "行气止痛",
      甘草: "调和诸药",
      生姜: "散寒和胃",
      金银花: "清热解毒",
      连翘: "清热散结",
      薄荷: "疏散风热",
      牛蒡子: "利咽透疹",
      黄芩: "清热燥湿",
      石膏: "清气分热",
      香薷: "解表化湿",
      藿香: "化湿和中",
      佩兰: "芳香化湿",
      麦冬: "养阴润肺",
      沙参: "养阴清肺"
    },
    herbDosage: {
      荆芥: "9g",
      防风: "9g",
      羌活: "6g",
      独活: "6g",
      柴胡: "9g",
      前胡: "9g",
      桔梗: "6g",
      枳壳: "6g",
      茯苓: "12g",
      川芎: "6g",
      甘草: "3g",
      生姜: "3片",
      金银花: "12g",
      连翘: "12g",
      薄荷: "6g",
      牛蒡子: "9g",
      黄芩: "9g",
      石膏: "15g",
      香薷: "9g",
      藿香: "9g",
      佩兰: "9g",
      麦冬: "12g",
      沙参: "12g"
    },
    coldHerbs: ["金银花", "连翘", "石膏", "黄芩"],
    patent: ["风寒感冒颗粒", "荆防颗粒", "感冒清热颗粒"],
    inquiryTips: {
      汗: "无汗 → 支持风寒束表判断。",
      鼻涕: "清涕 → 更偏寒象，可用于鉴别风热感冒。",
      清涕: "清涕 → 更偏寒象，可用于鉴别风热感冒。",
      咽喉: "明显咽痛、红肿不突出 → 暂不支持典型风热咽痛。",
      咽痛: "明显咽痛、红肿不突出 → 暂不支持典型风热咽痛。",
      胃肠: "胃肠症状不明显 → 暑湿感冒证据不足。",
      怕冷: "恶寒较重 → 更支持风寒外束。",
      痰: "痰白清稀 → 更偏寒象，不支持痰热主线。",
      口渴: "不明显口渴 → 热象证据不足。",
      舌象: "苔薄白 → 更支持表寒或寒热不重。",
      脉象: "脉浮紧 → 风寒束表的重要证据。"
    },
    choiceTips: {
      风寒感冒: "当前证据中“恶寒明显、无汗、清涕、痰白、苔薄白、脉浮紧”与风寒感冒较吻合。",
      风热感冒: "请注意：本案虽有低热和咽部不适，但缺少明显咽痛、口渴、黄涕、黄痰、苔黄、脉浮数等风热证据。",
      暑湿感冒: "请注意：本案胃肠症状不明显，暑湿证据不足。需要关注是否有腹胀、恶心、腹泻、身重困倦等表现。",
      气虚感冒: "请注意：本案起病较急，外感风寒证据更明显，暂未见反复感冒、自汗、乏力气短等气虚主线。",
      阴虚感冒: "请注意：本案暂未见明显口干咽燥、潮热盗汗、舌红少苔等阴虚证据。",
      "辛温解表，宣肺散寒": "治法方向与风寒束表较匹配。",
      "辛凉解表，疏风清热": "该治法更偏风热证，本案热象不突出，需谨慎。",
      "荆防败毒散加减": "该方方向偏疏风散寒，适合外感风寒、头痛身痛、无汗等表现。",
      "麻黄汤加减": "麻黄汤也在风寒节点下，偏风寒表实、无汗身痛；但本案普通感冒训练中，荆防败毒散方向更贴合。",
      "桂枝汤加减": "桂枝汤也可连到风寒节点，但偏表虚有汗、营卫不和。本案无汗，匹配度不如荆防败毒散。",
      "银翘散加减": "银翘散经风热节点连接，多用于风热表证。本案风寒证据更明显。",
      "桑菊饮加减": "桑菊饮偏风热咳嗽方向，本案痰白清稀、恶寒明显，不是主线。",
      "新加香薷饮加减": "新加香薷饮经暑湿节点连接，本案胃肠湿阻证据不足。",
      "白虎加人参汤": "白虎加人参汤偏气分热盛、气津两伤，本案无高热汗出、口渴、脉洪大等证据。"
    },
    herbTips: {
      荆芥: "荆芥：疏风解表，适合外感风寒初起。",
      防风: "防风：祛风解表，可帮助解除表邪。",
      羌活: "羌活：散寒止痛，适合头痛、项背酸痛较明显。",
      前胡: "前胡：宣肺化痰，适合外感兼咳痰。",
      甘草: "甘草：调和诸药，常用于方中协调药性。",
      金银花: "金银花偏清热解毒，多用于风热或热毒，本案热象不明显，不宜作为主线。",
      连翘: "连翘偏清热散结，多用于风热方向，本案风寒证据更强。",
      石膏: "石膏偏清气分实热，本案无高热、口渴、苔黄等明显实热证据。",
      藿香: "藿香偏化湿和中，本案胃肠湿困表现不突出。",
      麦冬: "麦冬偏养阴润肺，本案暂未见明显阴虚燥热证据。"
    },
    patentTips: {
      风寒感冒颗粒: "更符合风寒外束、恶寒重、清涕、无汗等方向。",
      荆防颗粒: "偏疏风散寒，适合本案风寒表证方向。",
      感冒清热颗粒: "常用于风寒感冒兼头痛发热、恶寒身痛等表现，方向较匹配。",
      银翘解毒丸: "偏风热表证，适用于发热重、咽痛、口渴、黄涕等情况。本案不是首选。",
      桑菊感冒片: "偏风热咳嗽方向，本案痰白清稀、寒象更明显。",
      藿香正气水: "偏暑湿或外感夹湿，适用于胃肠症状明显者。本案胃肠症状不突出。",
      双黄连口服液: "偏风热或热毒方向，本案风寒证据更强。",
      连花清瘟胶囊: "偏热毒、风热夹毒等方向，本案不是首选。"
    }
  },
  prescriptions: {
    "逍遥散合酸枣仁汤加减": ["柴胡", "白芍", "当归", "茯苓", "白术", "酸枣仁", "夜交藤", "合欢皮", "炙甘草"],
    "归脾汤加减": ["党参", "黄芪", "白术", "茯神", "酸枣仁", "龙眼肉", "当归", "远志", "炙甘草"],
    "黄连阿胶汤加减": ["黄连", "黄芩", "阿胶", "白芍", "鸡子黄", "麦冬", "生地"],
    "温胆汤加减": ["半夏", "竹茹", "陈皮", "枳实", "茯苓", "酸枣仁", "远志", "炙甘草"],
    "安神定志丸加减": ["党参", "茯苓", "茯神", "远志", "石菖蒲", "龙齿", "酸枣仁"]
  },
  prescription: {
    settings: ["模拟服药：3天", "每日反馈：开启", "风险观察：脾胃反应、睡眠变化、情绪变化"]
  },
  feedbackMap: {
    liver: {
      label: "肝郁气滞 + 逍遥散合酸枣仁汤：逐步好转",
      days: [
        { day: "第1天反馈", items: ["入睡时间略缩短", "胸闷叹息减少", "仍有多梦", "食欲无明显变化"], tip: "方向基本正确，需继续观察心神不宁与血虚因素" },
        { day: "第2天反馈", items: ["入睡继续改善", "经前烦躁减轻", "但晨起乏力略明显", "舌苔仍薄白"], tip: "疏肝方向有效，但需注意养血安神力量是否充足" },
        { day: "第3天反馈", items: ["入睡明显改善", "多梦减少", "情绪较平稳", "乳房胀痛减轻"], tip: "治疗方向较匹配，可进入复盘" }
      ]
    },
    spleen: {
      label: "心脾两虚 + 归脾汤：乏力略改善，肝郁相关症状改善有限",
      days: [
        { day: "第1天反馈", items: ["晨起乏力略减", "入睡仍慢", "胸闷叹息变化不明显", "经前烦躁仍在"], tip: "补益心脾对乏力有帮助，但情志郁结证据仍需处理" },
        { day: "第2天反馈", items: ["精神稍稳", "多梦略减", "经前乳胀仍明显", "脉弦仍较突出"], tip: "当前选方对心脾不足有照顾，但与主导病机匹配度不足" },
        { day: "第3天反馈", items: ["乏力继续略改善", "胸胁不舒仍反复", "入睡改善有限", "情绪波动仍明显"], tip: "建议复盘：本案不宜只按心脾两虚处理，应补充疏肝解郁思路" }
      ]
    },
    yinFire: {
      label: "阴虚火旺 + 黄连阿胶汤：清热滋阴方向可能偏重",
      days: [
        { day: "第1天反馈", items: ["口干略减", "入睡改善不明显", "胃口下降", "大便较前偏稀"], tip: "清热滋阴后津液感略有改善，但热象证据不足，需留意脾胃反应" },
        { day: "第2天反馈", items: ["晨起乏力增加", "胃纳一般", "经前烦躁仍有", "舌苔仍薄白"], tip: "方药方向可能偏寒偏重，未抓住情志郁结主线" },
        { day: "第3天反馈", items: ["多梦仍在", "腹部不适感增加", "口干不明显", "情绪压力未明显缓解"], tip: "建议复盘：不能仅凭烦躁判断阴虚火旺，应回看舌脉与胸胁证据" }
      ]
    },
    phlegm: {
      label: "痰热扰心 + 温胆汤：胸闷略缓，失眠改善有限",
      days: [
        { day: "第1天反馈", items: ["胸闷略缓", "入睡仍慢", "多梦无明显变化", "口苦痰多不明显"], tip: "化痰理气带来轻微胸闷改善，但痰热证据不足" },
        { day: "第2天反馈", items: ["胃脘较舒", "经前乳胀仍明显", "烦躁仍与压力相关", "舌苔未见黄腻"], tip: "当前反馈提示中焦气机可兼顾，但不是本案主轴" },
        { day: "第3天反馈", items: ["失眠改善有限", "叹息仍反复", "情绪波动仍在", "食欲尚可"], tip: "建议复盘：温胆汤思路可作鉴别，但本案更需辨清肝郁扰神" }
      ]
    },
    default: {
      label: "当前组合未形成稳定方证匹配，系统生成观察型反馈",
      days: [
        { day: "第1天反馈", items: ["主要症状波动不大", "入睡仍慢", "情绪压力仍明显", "食欲无明显变化"], tip: "请回看问诊证据，确认主证与参考方是否互相支持" },
        { day: "第2天反馈", items: ["睡眠改善有限", "胸胁不舒仍有", "舌脉变化不明显", "患者对方向仍有疑问"], tip: "当前辨证与选方匹配度一般，建议申请数字名医会诊" },
        { day: "第3天反馈", items: ["整体改善不稳定", "关键症状仍反复", "未见明确不良趋势", "需要复盘证据链"], tip: "建议从情志、月经前乳胀、脉弦三个证据重新梳理" }
      ]
    }
  },
  coldFeedbackMap: {
    correct: {
      label: "风寒感冒 + 荆防败毒散 + 风寒类中成药：方向匹配",
      days: [
        { day: "第1天反馈", items: ["怕冷减轻", "鼻塞和清涕减少", "头痛身痛稍缓", "咳嗽仍有少量白稀痰"], tip: "辛温解表方向基本匹配，外寒逐渐松解，继续观察汗出与咳痰变化。" },
        { day: "第2天反馈", items: ["恶寒明显减轻", "清涕减少", "头痛身痛继续缓解", "精神较前好转"], tip: "风寒表证较前缓解，方证方向较合适。" },
        { day: "第3天反馈", items: ["鼻塞基本缓解", "身痛明显减轻", "低热消退", "睡眠恢复"], tip: "本案模拟恢复较好，可进入复盘。" }
      ]
    },
    mahuang: {
      label: "风寒感冒 + 麻黄汤加减：同属风寒网络，但方证偏峻",
      days: [
        { day: "第1天反馈", items: ["怕冷略减", "头痛身痛稍缓", "鼻塞清涕仍有", "患者感觉发散力量偏强"], tip: "麻黄汤与无汗、恶寒、身痛相关，但本案普通感冒更适合先考虑荆防败毒散方向。" },
        { day: "第2天反馈", items: ["身痛继续减轻", "咳嗽和清涕改善一般", "精神略恢复"], tip: "图谱路径同属风寒，但方剂颗粒度需要进一步区分：表实无汗与普通风寒感冒不完全等同。" }
      ]
    },
    guizhi: {
      label: "风寒感冒 + 桂枝汤加减：风寒相关，但有汗/无汗证据不合",
      days: [
        { day: "第1天反馈", items: ["怕冷改善有限", "仍无明显出汗", "鼻涕仍清稀", "身痛仍在"], tip: "桂枝汤偏表虚有汗、营卫不和；本案无汗更明显，方证匹配不足。" },
        { day: "第2天反馈", items: ["鼻塞仍有", "身痛略缓", "恶寒仍反复"], tip: "需回到症候节点：无汗、恶寒重、脉浮紧，更支持荆防败毒散或风寒表实相关路径。" }
      ]
    },
    windHeat: {
      label: "风热感冒 + 银翘散 / 银翘解毒丸：辛凉清解方向偏离",
      days: [
        { day: "第1天反馈", items: ["咽部不适略减", "怕冷、清涕、身痛没有明显改善", "胃口略下降"], tip: "辛凉清解方向可能偏离。本案风寒证据更明显，如恶寒重、无汗、清涕、苔薄白、脉浮紧。" },
        { day: "第2天反馈", items: ["仍怕冷", "鼻涕仍清稀", "腹部发凉", "精神一般"], tip: "需重新回到风寒与风热证据鉴别。" }
      ]
    },
    dampSummer: {
      label: "暑湿感冒方向：胃肠症状不突出，暑湿证据不足",
      days: [
        { day: "第1天反馈", items: ["胃口略好", "怕冷、清涕、头痛身痛改善有限"], tip: "本案胃肠症状不突出，暑湿证据不足。应重新关注起病诱因、汗出、鼻涕和脉象。" },
        { day: "第2天反馈", items: ["鼻塞仍在", "清涕仍明显", "身痛改善有限"], tip: "暑湿方向不是本案主线，建议回看风寒表证。" }
      ]
    },
    coldHerbs: {
      label: "寒凉清热药物选择偏多：可能影响辛温解表方向",
      days: [
        { day: "第1天反馈", items: ["咽部不适略缓", "怕冷仍明显", "清涕和身痛改善有限", "胃口略受影响"], tip: "你选择了较多偏寒凉清热药物，但本案热象不明显，可能影响辛温解表方向。建议回到症候证据重新判断。" },
        { day: "第2天反馈", items: ["仍有畏寒", "鼻涕仍清稀", "腹部偏凉", "精神一般"], tip: "寒凉药物不宜作为本案主线。" }
      ]
    },
    groupMismatch: {
      label: "药组理解还不完整：主力药组搭配需要再校一遍",
      days: [
        { day: "第1天反馈", items: ["怕冷略减", "鼻塞和清涕改善不稳定", "咳嗽时轻时重", "整体恢复节奏偏慢"], tip: "你已经抓到了一部分方向，但主力药组搭配还不够完整。建议回看风寒解表、宣肺化痰和和中辅助三组的分工。" },
        { day: "第2天反馈", items: ["头痛身痛仍有残留", "咽部不适时有反复", "精神恢复一般", "病情变化不够顺畅"], tip: "这类反馈常提示：主线抓到了，但药组之间的配合还需要更清楚。" }
      ]
    }
  },
  assessmentMap: {
    liver: {
      score: 82,
      tag: "方向基本正确",
      dimensions: [["辨证方向", 85], ["关键证据抓取", 78], ["方证匹配", 82], ["用药安全性", 88]],
      comment: "你抓住了肝郁气滞的核心证据：压力大、叹气、经前乳胀、脉弦。但本案失眠已持续半年，后续还需要观察心神不宁与血虚因素。",
      risks: ["入睡时间是否缩短", "多梦是否减少", "晨起乏力是否出现", "胸胁胀满是否缓解"]
    },
    spleen: {
      score: 72,
      tag: "证型部分匹配",
      dimensions: [["辨证方向", 70], ["关键证据抓取", 68], ["方证匹配", 72], ["用药安全性", 84]],
      comment: "你关注到了失眠日久可能存在心脾不足，这是有价值的。但本案叹气、经前乳胀、脉弦更支持肝郁气滞，若只从心脾两虚入手，可能遗漏情志郁结主线。",
      risks: ["乏力是否改善", "胸闷叹息是否仍明显", "经前乳胀是否缓解", "入睡时间是否真正缩短"]
    },
    yinFire: {
      score: 63,
      tag: "方向可能偏重",
      dimensions: [["辨证方向", 58], ["关键证据抓取", 62], ["方证匹配", 60], ["用药安全性", 72]],
      comment: "你捕捉到了烦躁与口干，但本案口苦、舌红、苔黄等热象并不突出。黄连阿胶汤方向偏清热滋阴，入门训练中需要特别留意胃口下降、大便偏稀等模拟风险。",
      risks: ["胃口是否下降", "大便是否偏稀", "口干是否真正明显", "晨起乏力是否加重"]
    },
    phlegm: {
      score: 68,
      tag: "鉴别思路可保留",
      dimensions: [["辨证方向", 66], ["关键证据抓取", 65], ["方证匹配", 68], ["用药安全性", 78]],
      comment: "你考虑到胸闷与气机不畅，但痰热扰心需要更多痰多、口苦、苔黄腻等证据支持。本案温胆汤可作为鉴别思路，但主线仍应回到肝郁扰神。",
      risks: ["胸闷是否缓解", "失眠改善是否有限", "苔象是否出现黄腻", "情绪压力是否仍是主因"]
    },
    default: {
      score: 60,
      tag: "需要回看证据",
      dimensions: [["辨证方向", 58], ["关键证据抓取", 62], ["方证匹配", 56], ["用药安全性", 70]],
      comment: "当前证型与选方尚未形成清晰匹配。建议回到问诊证据，优先确认情志、经前乳胀、脉弦、舌苔与二便信息之间的关系，再决定治法与参考方。",
      risks: ["核心症状是否改善", "是否出现新的不适", "方证是否互相支持", "是否需要申请数字名医会诊"]
    }
  },
  experts: [
    {
      id: "jingfang",
      title: "经方派数字名医",
      focus: "方证对应、少阳枢机、经方加减",
      opinion: ["本案可借鉴少阳枢机思路，但目前少阳证据不足。", "问诊中未见明显口苦、咽干、寒热往来，不宜机械套用经方。", "方证对应应先服务于核心证据：叹气、经前乳胀、情绪波动、脉弦。", "后续若出现口苦、胸胁苦满加重、苔黄等，再重新评估少阳与郁热。"]
    },
    {
      id: "emotion",
      title: "情志病数字名医",
      focus: "肝郁、心神、睡眠与情绪关系",
      opinion: ["本案肝郁扰神特征较明显：叹气、经前乳胀、情绪波动、脉弦。", "失眠并非单纯心神失养，而是情志郁结影响心神。", "疏肝解郁与养血安神应并行，既处理气机，也照顾多梦与心神不宁。", "若后续乏力、心悸明显，应考虑肝郁兼血虚或心脾不足。"]
    },
    {
      id: "spleen",
      title: "脾胃派数字名医",
      focus: "中焦运化、药后反应、虚实夹杂",
      opinion: ["本案食欲一般、大便不稀，脾胃并非当前最突出矛盾，但用药后仍需观察中焦。", "若模拟服药中出现乏力、腹胀加重、胃口下降，应注意方药对脾胃的影响。", "酸枣仁、夜交藤等安神药可帮助睡眠，但配伍中仍需兼顾运化。", "复诊时建议同时追问腹胀、纳食、大便和晨起精神。"]
    }
  ],
  review: {
    judgment: ["主证：肝郁气滞", "治法：疏肝解郁，养血安神", "参考方：逍遥散合酸枣仁汤加减"],
    evidence: [
      "入睡困难、多梦：心神不宁",
      "情绪压力大、容易叹气：肝气郁结",
      "经前乳房胀痛：肝郁气滞",
      "脉弦：肝郁证据",
      "苔薄白、大便不稀：热象与痰热证据不足"
    ],
    mistakes: [
      "不能一看到失眠就直接判断为心脾两虚",
      "不能只看烦躁就判断为阴虚火旺",
      "本案热象不明显，暂不宜过度清热",
      "需要区分“肝郁扰神”和“心血不足”"
    ],
    comment: "你的辨证方向基本正确，能够抓住情志、月经前乳胀、脉弦等关键证据。但需要进一步关注失眠背后的兼证，比如血虚、心神不宁，以及后续是否出现化火趋势。",
    growth: [["肝郁辨证", "+10"], ["失眠专病", "+8"], ["问诊完整度", "+6"], ["方证匹配", "+7"]]
  },
  tabs: [
    ["home", "医馆", "🏠"],
    ["training", "训练", "🩺"],
    ["cases", "病案", "📚"],
    ["growth", "成长", "🌱"]
  ],
  growthProfile: {
    identity: "见习医者",
    receivedPatients: "2 位",
    savedRecords: "2 份",
    nextLevel: "独立坐诊",
    upgradeNeed: "还需完成 3 个复诊病例",
    weeklyGoal: "完成 3 个感冒辨证病例",
    badges: ["风寒初辨", "问诊入门"],
    nextChallenge: "风热感冒对照案"
  },
  disclaimer: "本系统仅用于中医教学训练，所有病例均为虚拟模拟，不用于真实患者诊疗或用药建议。"
};

const state = {
  page: "home",
  activeTab: "home",
  activeCaseId: "lin",
  selections: JSON.parse(JSON.stringify(DATA.diagnosis)),
  coldSelections: JSON.parse(JSON.stringify(DATA.coldDiagnosis)),
  activeExpert: null,
  savedCase: false,
  savedCases: { lin: false, zhou: false, wang: false },
  showCaseSummary: false,
  showAiPrompts: false,
  showInfoPanel: false,
  showPendingInfo: false,
  activeEvidenceGuide: "",
  draftInquiryQuestion: "",
  coldLearningStep: 1,
  coldGroupSelections: [],
  coldGroupSubmitted: false,
  coldExpandedHerbPanels: [],
  coldShowSyndromePathCard: false,
  coldShowFormulaPathCard: false,
  coldMeaningViewed: false,
  coldDoseConfirmed: false,
  coldChoiceTip: "",
  coldHerbTip: "",
  coldPatentTip: "",
  pendingUnlockCue: null,
  caseStates: {
    lin: { inquiryRecords: [], unlockedInfo: [], unlockedEvidence: {}, hintProgress: {}, nextRecordId: 1 },
    zhou: { inquiryRecords: [], unlockedInfo: [], unlockedEvidence: {}, hintProgress: {}, nextRecordId: 1 },
    wang: { inquiryRecords: [], unlockedInfo: [], unlockedEvidence: {}, hintProgress: {}, nextRecordId: 1 }
  }
};

const app = document.querySelector("#app");
const tabBar = document.querySelector("#tabBar");
const toast = document.querySelector("#toast");

function render(page = state.page, options = {}) {
  const scrollY = window.scrollY;
  state.page = page;
  state.activeTab = tabForPage(page);
  if (page !== "patient") state.activeEvidenceGuide = "";
  app.classList.toggle("no-page-motion", Boolean(options.preserveScroll || options.scrollInquiryBottom || options.noPageMotion));
  app.innerHTML = views[page] ? views[page]() : views.home();
  app.classList.toggle("inquiry-mode", page === "inquiry");
  renderTabs();
  bindPageActions();
  if (page === "inquiry" && state.pendingUnlockCue) {
    playInfoUnlockEffect(state.pendingUnlockCue);
    state.pendingUnlockCue = null;
  }
  if (options.scrollInquiryBottom) {
    scrollInquiryToBottom();
  } else if (options.preserveScroll) {
    window.scrollTo({ top: scrollY });
  } else {
    window.scrollTo({ top: 0, behavior: "smooth" });
  }
}

function tabForPage(page) {
  if (["home"].includes(page)) return "home";
  if (["training", "waiting", "patient", "inquiry", "diagnosis", "coldDosage", "prescription", "assessment", "observation", "consult"].includes(page)) return "training";
  if (["cases", "review"].includes(page)) return "cases";
  return "growth";
}

function renderTabs() {
  tabBar.classList.toggle("hidden", state.page === "inquiry");
  tabBar.innerHTML = DATA.tabs.map(([id, label, icon]) => `
    <button class="tab-button ${state.activeTab === id ? "active" : ""}" data-route="${id}">
      <span class="icon">${icon}</span>
      <span>${label}</span>
    </button>
  `).join("");
  tabBar.querySelectorAll("button").forEach(button => {
    button.addEventListener("click", () => render(button.dataset.route));
  });
}

const views = {
  home() {
    const c = DATA.clinic;
    const recommended = DATA.patients.find(patient => patient.id === "zhou");
    return `
      <article class="page">
        <section class="hero">
          <p class="eyebrow">今日开诊</p>
          <h1>${DATA.brand}</h1>
          <p class="hero-copy">我的${c.name} · 今日开诊</p>
          <p class="hero-copy">${c.slogan}</p>
          <span class="level-pill">医馆等级：${c.level}</span>
        </section>
        <section class="card">
          <div class="section-head tight"><h2>今日任务</h2><span class="tiny-pill">开诊中</span></div>
          <div class="summary-list">
            <div>完成 1 次问诊取证</div>
            <div>复盘 1 份已完成病案</div>
            <div>解锁风寒 / 风热鉴别能力</div>
          </div>
        </section>
        <section class="card risk-card">
          <div class="patient-top">
            <div>
              <h3>复诊风险提醒</h3>
              <p class="muted">${c.reminder.patient}｜服药第2天</p>
            </div>
            <span class="badge warn">需复诊判断</span>
          </div>
          <p class="complaint">反馈：${c.reminder.feedback}</p>
          <div class="system-tip">系统建议：请判断是否需要调整方药</div>
          <button class="soft-btn" data-toast="已打开陈女士服药后教学反馈">查看反馈</button>
        </section>
        <div class="section-head"><h2>今日推荐病例</h2><span class="tiny-pill">感冒鉴别</span></div>
        ${patientCard(recommended)}
        <section class="card">
          <h3>医馆成长</h3>
          <div class="summary-list">
            <div>医馆声望：128</div>
            <div>已收入病案：2 份</div>
            <div>已获得徽章：风寒初辨、情志初辨</div>
            <div>当前身份：见习医者</div>
          </div>
        </section>
        <p class="notice">${DATA.disclaimer}</p>
      </article>
    `;
  },
  training() {
    return `
      <article class="page">
        <section class="hero">
          <p class="eyebrow">${DATA.brand}</p>
          <h1>训练</h1>
          <p class="hero-copy">选择一位虚拟患者，完成一次模拟坐诊。</p>
          <span class="level-pill">今日候诊 3 人</span>
        </section>
        <h2>虚拟患者列表</h2>
        ${DATA.patients.map(patientCard).join("")}
        <p class="notice">${DATA.disclaimer}</p>
      </article>
    `;
  },
  waiting() {
    return views.training();
  },
  patient() {
    const p = getActivePatient();
    const detail = getPatientDetailView();
    const progress = getEvidenceProgress();
    return `
      <article class="page">
        ${isColdCase() ? renderColdStageProgress("问诊取证") : ""}
        ${pageTitle(`${p.name}，${p.age}`, `身份：${p.identity}｜主诉：${p.complaint}`)}
        ${patientProfileCard(p, detail.observable)}
        ${renderEntryStatusCard()}
        <section class="card">
          <div class="section-head compact patient-layer-head">
            <h2>第一层：主诉</h2>
            <span class="tiny-pill">先形成初印象</span>
          </div>
          <p class="complaint">${detail.chief}</p>
        </section>
        <section class="card">
          <div class="section-head compact patient-layer-head">
            <h2>第二层：可观察信息</h2>
            <span class="tiny-pill">先看其神与外在状态</span>
          </div>
          <p class="muted detail-observe">${detail.observable}</p>
        </section>
        <section class="card">
          <div class="section-head compact patient-layer-head">
            <h2>第三层：待追问解锁</h2>
            <span class="tiny-pill">先取证再辨证</span>
          </div>
          <div class="evidence-progress-block">
            <p class="evidence-progress-line">已获得关键证据：${progress.unlockedCount}/${progress.total}</p>
            ${progress.unlockedCount >= 5 ? `<p class="evidence-ready-line">证据已基本足够，可以进入初步辨证。</p>` : ""}
          </div>
          <div class="evidence-status-list">
            ${renderPatientEvidenceStatus()}
          </div>
          ${renderEvidenceGuidePanel()}
        </section>
        <div class="button-row">
          <button class="${progress.unlockedCount < 5 ? "primary-btn" : "soft-btn"}" data-route="inquiry">继续问诊</button>
          <button class="${progress.unlockedCount < 5 ? "ghost-btn" : "primary-btn"}" ${progress.unlockedCount < 5 ? `data-guard-diagnosis` : `data-route="diagnosis"`}>开始辨证</button>
        </div>
      </article>
    `;
  },
  inquiry() {
    const p = getActivePatient();
    const inquiry = getInquiryAnalysis();
    const progress = getEvidenceProgress();
    const topic = isColdCase() ? "感冒初诊" : "失眠初诊";
    const masteredInfo = getMasteredInfoItems();
    return `
      <article class="page inquiry-page">
        <section class="inquiry-topbar">
          <div>
            <h1>${p.name}｜${topic}</h1>
            <p>已追问 ${inquiry.total} 个问题｜已获得关键证据 ${progress.unlockedCount}/${progress.total}</p>
          </div>
          <div class="inquiry-top-actions">
            <button class="ghost-btn" data-route="patient">返回</button>
            <button class="soft-btn" data-toggle-ai-prompts>AI建议补漏</button>
            <button class="ghost-btn info-entry-btn" data-toggle-info-panel>已获得证据 <span class="tiny-count">${masteredInfo.length}</span></button>
          </div>
        </section>
        <section class="inquiry-thread" id="inquiryThread">
          ${renderInquiryConversation()}
          ${renderDiagnosisReadyCard(progress)}
          <div id="inquiryEnd"></div>
        </section>
        <section class="inquiry-composer" aria-label="模拟问诊输入">
          <input id="freeInquiryInput" class="inquiry-input" value="${escapeHTML(state.draftInquiryQuestion || "")}" placeholder="${isColdCase() ? "向虚拟病人追问..." : "向虚拟病人追问..."}">
          <button class="ghost-btn voice-btn" data-voice-inquiry>语音</button>
          <button class="primary-btn send-btn" data-send-inquiry>发送</button>
        </section>
        ${state.showAiPrompts ? renderAiPromptDrawer() : ""}
        ${state.showInfoPanel ? renderInquiryInfoPanel() : ""}
      </article>
    `;
  },
  diagnosis() {
    if (isColdCase()) return renderColdDiagnosis();
    return `
      <article class="page">
        ${pageTitle("请完成你的辨证判断", "卡片式选择会保留你的当前判断")}
        <div class="diagnosis-toolbar">
          <button class="ghost-btn" data-route="inquiry">返回问诊</button>
          <button class="soft-btn" data-toggle-case-summary>${state.showCaseSummary ? "收起病案" : "查看病案"}</button>
        </div>
        ${state.showCaseSummary ? renderCaseSummary() : ""}
        <section class="card">
          ${Object.entries(state.selections).map(([group, config]) => `
            <div class="form-group">
              <div class="form-title"><span>${group}</span><span class="muted">${config.multi ? "可多选" : "单选"}</span></div>
              <div class="chip-grid">
                ${config.options.map(option => `<button class="chip ${config.selected.includes(option) ? "selected" : ""}" data-group="${group}" data-option="${option}">${option}</button>`).join("")}
              </div>
            </div>
          `).join("")}
          <button class="primary-btn" data-route="assessment">提交处方</button>
        </section>
      </article>
    `;
  },
  coldDosage() {
    return renderColdDosage();
  },
  assessment() {
    if (isColdCase()) return renderColdAssessment();
    const assessment = getAssessmentResult();
    const inquiry = getInquiryAnalysis();
    return `
      <article class="page">
        ${pageTitle("AI带教评估", "入门级病例 · 模拟服药前评估")}
        <section class="card assessment-hero">
          <div>
            <span class="badge green">${assessment.tag}</span>
            <h2>综合评分</h2>
          </div>
          <strong>${assessment.score}<span>分</span></strong>
        </section>
        <section class="card">
          <h3>评分维度</h3>
          <div class="score-list">
            ${assessment.dimensions.map(([name, value]) => `
              <div class="score-row">
                <div>
                  <span>${name}</span>
                  <strong>${value}</strong>
                </div>
                <div class="bar"><span style="width:${value}%"></span></div>
              </div>
            `).join("")}
          </div>
        </section>
        <section class="card">
          <h3>AI带教点评</h3>
          <p class="complaint">${assessment.comment}</p>
        </section>
        <section class="card">
          <h3>问诊表现点评</h3>
          <div class="summary-list">
            <div>${inquiry.pointComment}</div>
            <div>${inquiry.missingComment}</div>
            <div>${inquiry.weakComment}</div>
          </div>
        </section>
        <section class="card">
          <h3>风险观察点</h3>
          <div class="summary-list">${assessment.risks.map(item => `<div>${item}</div>`).join("")}</div>
        </section>
        <div class="button-row">
          <button class="ghost-btn" data-route="diagnosis">返回调整</button>
          <button class="primary-btn" data-route="observation">开始模拟服药</button>
        </div>
      </article>
    `;
  },
  prescription() {
    const prescriptionName = getSelected("参考方选择");
    const medicines = DATA.prescriptions[prescriptionName] || DATA.prescriptions["逍遥散合酸枣仁汤加减"];
    return `
      <article class="page">
        ${pageTitle("处方确认", "本页为虚拟教学开方，不用于真实用药")}
        <section class="card">
          <span class="badge green">参考方</span>
          <h2>${prescriptionName}</h2>
          <div class="medicine-list">${medicines.map(m => `<div class="medicine">${m}</div>`).join("")}</div>
        </section>
        <section class="card">
          <h3>服药设置</h3>
          <div class="summary-list">${DATA.prescription.settings.map(item => `<div>${item}</div>`).join("")}</div>
        </section>
        <button class="primary-btn" data-route="observation">开始模拟服药</button>
        <p class="notice">${DATA.disclaimer}</p>
      </article>
    `;
  },
  observation() {
    if (isColdCase()) return renderColdObservation();
    const feedback = getFeedbackScenario();
    const inquiry = getInquiryAnalysis();
    const p = getActivePatient();
    return `
      <article class="page">
        ${pageTitle("林女士 · 服药观察", "模拟服药后的每日教学反馈")}
        ${patientStateCard(p, "day1", "恢复中", "入睡略有改善，但仍多梦。")}
        <section class="card simulation-card">
          <span class="badge green">模拟生成</span>
          <h3>本次模拟反馈由你的辨证与选方生成</h3>
          <p class="complaint">${feedback.label}</p>
          <div class="system-tip">本次模拟依据：已纳入 ${inquiry.total} 条模拟问诊记录，命中 ${inquiry.keyHitCount} 个关键辨证问题；问诊完整度 ${inquiry.completenessScore} 分。</div>
        </section>
        <section class="timeline">
          ${feedback.days.map(day => `
            <div class="timeline-item">
              <section class="card">
                <h3>${day.day}</h3>
                <ul class="feedback-list">${day.items.map(item => `<li>${item}</li>`).join("")}</ul>
                <div class="system-tip">系统提示：${day.tip}</div>
              </section>
            </div>
          `).join("")}
        </section>
        <div class="button-row">
          <button class="ghost-btn" data-route="consult">申请数字名医会诊</button>
          <button class="primary-btn" data-route="review">查看复盘</button>
        </div>
      </article>
    `;
  },
  consult() {
    const activeExpert = DATA.experts.find(expert => expert.id === state.activeExpert);
    return `
      <article class="page">
        ${pageTitle("数字名医会诊", "选择不同学派视角，获得教学型会诊意见")}
        ${DATA.experts.map(expert => `
          <section class="card expert-card ${state.activeExpert === expert.id ? "selected-expert" : ""}">
            <div class="patient-top">
              <div>
                <h3>${expert.title}</h3>
                <p class="complaint">关注：${expert.focus}</p>
              </div>
              <span class="avatar">会</span>
            </div>
            <button class="${state.activeExpert === expert.id ? "primary-btn" : "soft-btn"}" data-expert="${expert.id}">请他会诊</button>
          </section>
        `).join("")}
        <section class="card consult-opinion ${activeExpert ? "show" : ""}" id="consultOpinion">
          <span class="badge green">会诊意见</span>
          <h3>${activeExpert ? activeExpert.title : "请选择数字名医"}</h3>
          <div class="summary-list">${activeExpert ? activeExpert.opinion.map(item => `<div>${item}</div>`).join("") : ""}</div>
          <div class="button-row">
            <button class="ghost-btn" data-route="observation">返回服药观察</button>
            <button class="primary-btn" data-route="review">进入复盘</button>
          </div>
        </section>
      </article>
    `;
  },
  review() {
    if (isColdCase()) return renderColdReview();
    const inquiry = getInquiryAnalysis();
    const p = getActivePatient();
    return `
      <article class="page">
        ${pageTitle("林女士病案复盘", "从证据、易错点和能力成长完成闭环")}
        ${patientResultCard(p, "入睡明显改善，多梦减少，情绪较平稳。", "情志初辨")}
        ${reviewBlock("你的判断", DATA.review.judgment)}
        ${reviewBlock("关键证据", DATA.review.evidence)}
        <section class="card">
          <h3>问诊复盘</h3>
          <div class="summary-list">
            <div>本次你共追问了 ${inquiry.total} 个问题，其中 ${inquiry.keyHitCount} 个属于关键辨证问题。</div>
            <div>问诊完整度：${inquiry.completenessScore} 分。</div>
            <div>${inquiry.pointComment}</div>
            <div>${inquiry.missingComment}</div>
            <div>${inquiry.nextAdvice}</div>
          </div>
        </section>
        ${reviewBlock("易错点", DATA.review.mistakes)}
        <section class="card">
          <h3>AI带教点评</h3>
          <p class="complaint">${DATA.review.comment}</p>
        </section>
        <section class="card">
          <h3>能力成长</h3>
          <div class="growth-grid">${DATA.review.growth.map(([name, score]) => `<div class="growth-card"><span>${name}</span><strong>${score}</strong></div>`).join("")}</div>
        </section>
        <div class="button-row">
          <button class="ghost-btn" data-save-case>收入病案</button>
          <button class="primary-btn" data-route="training">继续接诊</button>
        </div>
      </article>
    `;
  },
  cases() {
    const lin = DATA.patients.find(p => p.id === "lin");
    const zhou = DATA.patients.find(p => p.id === "zhou");
    return `
      <article class="page">
        ${pageTitle("病案架", "已完成病案会沉淀为你的训练资产")}
        <section class="card case-card">
          ${patientVisual(lin, "recovered", "small")}
          <div class="patient-top">
            <div>
              <h3>林女士｜肝郁失眠案</h3>
              <p class="muted">${state.savedCases.lin ? "已收入小满医馆病案架" : "待收入病案"}</p>
            </div>
            <span class="badge green">情志初辨</span>
          </div>
          <div class="summary-list"><div>最终判断：肝郁气滞</div><div>模拟结果：入睡改善，多梦减少，情绪较平稳</div><div>获得徽章：情志初辨</div></div>
          <button class="soft-btn" data-case="lin" data-route="review">查看复盘</button>
        </section>
        <section class="card case-card">
          ${patientVisual(zhou, "recovered", "small")}
          <div class="patient-top">
            <div>
              <h3>周同学｜风寒感冒案</h3>
              <p class="muted">${state.savedCases.zhou ? "已收入小满医馆病案架" : "待收入病案"}</p>
            </div>
            <span class="badge green">风寒初辨</span>
          </div>
          <div class="summary-list"><div>最终判断：风寒感冒</div><div>模拟结果：第3天鼻塞缓解，身痛减轻，低热消退</div><div>获得徽章：风寒初辨</div></div>
          <button class="soft-btn" data-case="zhou" data-route="review">查看复盘</button>
        </section>
        <section class="card">
          <h3>累计病案</h3>
          <div class="stats-grid">${DATA.clinic.stats.map(([label, value]) => `<div class="stat"><strong style="color:#183e34">${value}</strong><span style="color:#637469">${label}</span></div>`).join("")}</div>
        </section>
      </article>
    `;
  },
  growth() {
    const skills = [["风寒辨证", 74], ["风寒/风热鉴别", 70], ["问诊完整度", 78], ["方药匹配", 72]];
    const profile = DATA.growthProfile;
    return `
      <article class="page">
        ${pageTitle("成长", "你的中医临床思维能力卡")}
        <section class="card growth-profile">
          <div class="patient-top">
            <div>
              <span class="badge green">当前身份</span>
              <h2>${profile.identity}</h2>
            </div>
            <span class="avatar">阶</span>
          </div>
          <div class="summary-list">
            <div>下一等级：${profile.nextLevel}</div>
            <div>已接诊虚拟患者：${profile.receivedPatients}</div>
            <div>已收入病案：${profile.savedRecords}</div>
            <div>距离升级：${profile.upgradeNeed}</div>
            <div>本周目标：${profile.weeklyGoal}</div>
            <div>下一挑战：${profile.nextChallenge}</div>
          </div>
        </section>
        <section class="card">
          <h3>已获得徽章</h3>
          <div class="badge-row">${profile.badges.map(badge => `<span class="badge medal">${badge}</span>`).join("")}</div>
        </section>
        <section class="card">
          <h3>能力雷达</h3>
          <div class="growth-grid">${skills.map(([name, value]) => `
            <div class="growth-card">
              <span>${name}</span>
              <strong>${value}</strong>
              <div class="bar"><span style="width:${value}%"></span></div>
            </div>
          `).join("")}</div>
        </section>
        <section class="card">
          <h3>今日训练建议</h3>
          <p class="complaint">继续练习风寒、风热、暑湿感冒的证据区分，重点关注寒热轻重、汗出、鼻涕、痰色和胃肠症状。</p>
        </section>
      </article>
    `;
  }
};

function renderColdAssessment() {
  const result = getColdAssessmentResult();
  const inquiry = getInquiryAnalysis();
  return `
    <article class="page">
      ${renderColdStageProgress("方药匹配")}
      ${pageTitle("AI带教评估", "周同学感冒案 · 症候到方药完整判断")}
      <section class="card assessment-hero">
        <div>
          <span class="badge green">${result.tag}</span>
          <h2>综合评分</h2>
        </div>
        <strong>${result.score}<span>分</span></strong>
      </section>
      <section class="card">
        <h3>评分维度</h3>
        <div class="score-list">
          ${result.dimensions.map(([name, value]) => `
            <div class="score-row"><div><span>${name}</span><strong>${value}</strong></div><div class="bar"><span style="width:${value}%"></span></div></div>
          `).join("")}
        </div>
      </section>
      ${renderColdReasoningChain()}
      ${renderColdPathCompare()}
      ${reviewBlock("容易误判的干扰", ["体温 37.5℃：低热不等于风热，风寒感冒也可有轻度发热。", "咽部不适：不等于咽红肿痛，需要结合口渴、苔黄、脉浮数判断。", "咳嗽：不等于肺热，痰白清稀更支持寒象。"])}
      <section class="card"><h3>AI带教点评</h3><p class="complaint">${result.comment}</p></section>
      <section class="card"><h3>剂量教学说明</h3><p class="complaint">入门级病例重点考察方药组成匹配，剂量为系统教学参考。</p></section>
      <section class="card"><h3>问诊表现点评</h3><div class="summary-list"><div>${inquiry.pointComment}</div><div>${inquiry.missingComment}</div></div></section>
      <section class="card"><h3>中药配方点评</h3><p class="complaint">${result.herbComment}</p></section>
      <section class="card"><h3>中成药点评</h3><p class="complaint">${result.patentComment}</p></section>
      <section class="card"><h3>模拟观察预告</h3><div class="summary-list"><div>接下来，周同学将进入 3 天模拟服药观察。系统会根据你的证型、方剂、中药和中成药选择，生成不同的病情变化。</div><div>${getColdObservationPreview()}</div></div></section>
      <button class="soft-btn full" data-route="consult">申请数字名医会诊</button>
      <div class="button-row">
        <button class="ghost-btn" data-route="diagnosis">返回调整</button>
        <button class="primary-btn" data-route="observation">开始模拟服药</button>
      </div>
    </article>
  `;
}

function renderColdObservation() {
  const feedback = getColdFeedbackScenario();
  const inquiry = getInquiryAnalysis();
  const p = getActivePatient();
  return `
    <article class="page">
      ${renderColdStageProgress("模拟服药")}
      ${pageTitle("周同学 · 模拟服药观察", "根据证型、方剂、中药配方和中成药选择生成教学反馈")}
      ${patientStateCard(p, "day2", "好转", "怕冷减轻，清涕减少，头痛身痛缓解。")}
      <section class="card simulation-card">
        <span class="badge green">模拟生成</span>
        <h3>本次模拟反馈由你的辨证与方药选择生成</h3>
        <p class="complaint">${feedback.label}</p>
        <div class="system-tip">本次模拟依据：已纳入 ${inquiry.total} 条模拟问诊记录，命中 ${inquiry.keyHitCount} 个感冒辨证关键点；问诊完整度 ${inquiry.completenessScore} 分。</div>
      </section>
      <section class="timeline">
        ${feedback.days.map(day => `
          <div class="timeline-item"><section class="card"><h3>${day.day}</h3><ul class="feedback-list">${day.items.map(item => `<li>${item}</li>`).join("")}</ul><div class="system-tip">系统提示：${day.tip}</div></section></div>
        `).join("")}
      </section>
      <div class="button-row">
        <button class="ghost-btn" data-route="consult">申请数字名医会诊</button>
        <button class="primary-btn" data-route="review">查看复盘</button>
      </div>
    </article>
  `;
}

function renderColdReview() {
  const result = getColdAssessmentResult();
  const inquiry = getInquiryAnalysis();
  const selections = getColdStudentSelections();
  return `
    <article class="page">
      ${renderColdStageProgress("病案复盘")}
      ${pageTitle("周同学感冒案复盘", "症候 → 证型 → 方剂 → 中药配方 / 中成药")}
      ${patientResultCard(getActivePatient(), "鼻塞基本缓解，身痛明显减轻，低热消退。", "风寒初辨")}
      <section class="card"><h3>你的判断</h3><div class="summary-list">
        <div>证型：${selections.syndrome}</div><div>治法：${selections.method}</div><div>方剂：${selections.formula}</div>
        <div>中药配方：${formatHerbsWithDosage(selections.herbs) || "未选择"}</div><div>中成药：${selections.patent.join("、") || "未选择"}</div>
      </div></section>
      <section class="card"><h3>标准思路</h3><div class="summary-list"><div>证型节点：风寒</div><div>治法：辛温解表，宣肺散寒</div><div>本案首选方剂节点：荆防败毒散加减</div><div>图谱相关方剂：麻黄汤加减、桂枝汤加减也连向风寒，但需进一步根据无汗/有汗、表实/表虚区分。</div><div>中成药参考：风寒感冒颗粒 / 荆防颗粒 / 感冒清热颗粒</div></div></section>
      ${reviewBlock("关键证据", ["空调低温、吹风后发作：外感风寒诱因", "恶寒明显、发热轻：风寒特征", "无汗：表寒束表", "鼻流清涕：寒象", "痰白清稀：寒痰或肺气不宣", "舌淡红、苔薄白：寒热不重，偏表寒", "脉浮紧：风寒束表"])}
      ${reviewBlock("辨证路径复盘", ["第一步：从症候取证。恶寒明显、发热轻、流清涕、无汗、痰白清稀、苔薄白、脉浮紧。", "第二步：症候汇聚为证。这些症候在图谱中共同指向“风寒”节点。", "第三步：由证型节点进入方剂节点。风寒节点可连接荆防败毒散、麻黄汤、桂枝汤等多个方剂。", "第四步：本案进一步区分方剂。无汗、清涕、鼻塞喷嚏、头痛身痛支持荆防败毒散加减作为教学首选；麻黄汤偏表实发散，桂枝汤偏表虚有汗，本案匹配度较低。", "第五步：由方及药。荆芥、防风、羌活、独活负责疏风散寒；前胡、桔梗负责宣肺化痰；枳壳、茯苓、甘草、生姜辅助理气和中、调和药性。", "第六步：中成药匹配。风寒感冒颗粒、荆防颗粒、感冒清热颗粒更符合本案方向。"])}
      ${reviewBlock("干扰信息拆解", ["体温 37.5℃不等于风热，风寒感冒也可有轻度发热", "咽部不适不等于咽喉红肿热痛，需要继续追问疼痛程度、口渴、舌苔", "咳嗽不等于肺热，痰白清稀更支持寒象", "最近天气热不代表暑湿，本案没有明显腹胀、恶心、腹泻等湿阻中焦表现"])}
      <section class="card"><h3>问诊复盘</h3><div class="summary-list"><div>本次你共追问了 ${inquiry.total} 个问题，命中 ${inquiry.keyHitCount} 个感冒辨证关键点。</div><div>问诊完整度：${inquiry.completenessScore} 分。</div><div>${inquiry.pointComment}</div><div>${inquiry.missingComment}</div></div></section>
      <section class="card"><h3>方剂图谱复盘</h3><p class="complaint">按照辨证堂感冒图谱，本案先由“恶寒重、流清涕、无汗、痰白清稀、苔薄白、脉浮紧”等症候聚合到风寒节点，再从风寒节点比较荆防败毒散、麻黄汤、桂枝汤。荆防败毒散更适合本案普通风寒感冒的教学主线；麻黄汤虽同属风寒相关，但偏表实发散；桂枝汤偏表虚有汗，与本案无汗不合。银翘散、桑菊饮属于风热路径，新加香薷饮属于暑湿路径，白虎加人参汤偏热盛气津两伤，均不是本案主线。</p></section>
      <section class="card"><h3>中成药复盘</h3><p class="complaint">风寒感冒颗粒、荆防颗粒、感冒清热颗粒更符合本案方向。银翘解毒丸、桑菊感冒片、双黄连口服液偏风热或热毒方向，不作为本案首选。</p></section>
      ${reviewBlock("你本案最关键的进步", ["能够区分“低热”和“风热”的关系", "抓住了恶寒、无汗、清涕、痰白、脉浮紧等关键证据", "能够排除银翘散、桑菊感冒片等风热方向", "初步完成了从证型到方剂、药味、中成药的完整匹配"])}
      ${reviewBlock("下次遇到感冒，先问这 6 个问题", ["怕冷明显还是发热明显？", "有没有汗？", "鼻涕清还是黄？", "咽痛口渴明显吗？", "痰白还是痰黄？", "基础信息中的舌苔和脉象支持哪类证据？"])}
      <section class="card"><h3>能力成长</h3><div class="growth-grid">${[["风寒感冒辨证", "+10"], ["风寒 / 风热鉴别", "+8"], ["方药匹配", "+8"], ["中成药选择", "+6"], ["问诊完整度", "+6"]].map(([name, score]) => `<div class="growth-card"><span>${name}</span><strong>${score}</strong></div>`).join("")}</div></section>
      <button class="soft-btn full" data-route="consult">申请数字名医会诊</button>
      <div class="button-row"><button class="ghost-btn" data-toast="下一版将开放风热感冒对照训练。">挑战风热感冒案</button><button class="primary-btn" data-route="training">继续接诊</button></div>
    </article>
  `;
}

function renderColdReasoningChain() {
  const selections = getColdStudentSelections();
  const formulaMeta = DATA.coldAnswer.formulaGraph[selections.formula] || {};
  const evidence = "恶寒明显 / 无汗 / 鼻流清涕 / 痰白清稀 / 苔薄白 / 脉浮紧";
  if (selections.syndrome === "风热感冒") {
    return reasoningSection("症候—证—方—药推理链", [
      ["症候证据", evidence],
      ["你的证型判断", selections.syndrome || "未选择"],
      ["系统提示", "证据链断裂：当前症候更支持风寒，缺少明显咽痛、口渴、黄涕、黄痰、苔黄、脉浮数等风热证据。", "warn"]
    ]);
  }
  if (selections.syndrome === "暑湿感冒") {
    return reasoningSection("症候—证—方—药推理链", [
      ["症候证据", "恶寒明显 / 无汗 / 鼻流清涕 / 痰白清稀 / 脉浮紧"],
      ["你的证型判断", selections.syndrome || "未选择"],
      ["系统提示", "证据链偏离：本案胃肠症状不突出，缺少腹胀、恶心、腹泻、身重困倦等暑湿证据。", "warn"]
    ]);
  }
  const herbText = selections.herbs.length ? selections.herbs.slice(0, 8).join("、") + (selections.herbs.length > 8 ? "……" : "") : "未选择中药组成";
  const patentText = selections.patent.length ? selections.patent.join(" / ") : "未选择中成药";
  return reasoningSection("症候—证—方—药推理链", [
    ["症候证据", evidence],
    ["证型节点", `${selections.syndrome || "未选择"}（图谱对应：${formulaMeta.syndrome || "待判断"}）`],
    ["方剂节点", `${selections.formula || "未选择"}｜${formulaMeta.relation || "待判断"}`],
    ["中药与中成药", `${herbText}<br>${patentText}`]
  ], formulaMeta.note || "请根据图谱关系继续判断方剂与症候证据是否一致。");
}

function reasoningSection(title, nodes, comment = "") {
  return `
    <section class="card">
      <h3>${title}</h3>
      <div class="reasoning-chain">
        ${nodes.map((node, index) => `
          <div class="reasoning-node ${node[2] || ""}">
            <span>${node[0]}</span>
            <strong>${node[1]}</strong>
          </div>
          ${index < nodes.length - 1 ? `<div class="reasoning-arrow">↓</div>` : ""}
        `).join("")}
      </div>
      ${comment ? `<div class="coach-tip standalone">${comment}</div>` : ""}
    </section>
  `;
}

function renderColdPathCompare() {
  const answer = DATA.coldAnswer;
  const selections = getColdStudentSelections();
  const formulaMeta = answer.formulaGraph[selections.formula] || { relation: "需调整" };
  const herbHits = selections.herbs.filter(item => answer.herbs.includes(item)).length;
  const herbWrong = selections.herbs.filter(item => !answer.herbs.includes(item)).length;
  const patentHits = selections.patent.filter(item => answer.patent.includes(item)).length;
  const rows = [
    ["证型", selections.syndrome || "未选择", answer.syndrome, selections.syndrome === answer.syndrome ? "正确" : "偏离"],
    ["治法", selections.method || "未选择", answer.method, selections.method === answer.method ? "正确" : "偏离"],
    ["方剂", selections.formula || "未选择", "风寒节点下首选：荆防败毒散加减", selections.formula === answer.formula ? "正确" : formulaMeta.relation || "偏离"],
    ["中药", `选对 ${herbHits} 味${herbWrong ? `，混入 ${herbWrong} 味干扰药` : ""}`, "疏风散寒、宣肺解表为主", herbHits >= 8 && herbWrong <= 1 ? "较好" : "需调整"],
    ["中成药", selections.patent.join(" / ") || "未选择", "风寒类中成药", patentHits ? "正确" : "需调整"]
  ];
  return `
    <section class="card">
      <h3>你的路径 vs 标准路径</h3>
      <div class="path-compare">
        ${rows.map(row => `
          <div class="compare-item ${row[3] === "正确" || row[3] === "较好" ? "ok" : "warn"}">
            <div><span>${row[0]}</span><strong>${row[3]}</strong></div>
            <p>你的判断：${row[1]}</p>
            <p>标准路径：${row[2]}</p>
          </div>
        `).join("")}
      </div>
    </section>
  `;
}

function patientCard(patient) {
  const disabled = patient.id === "wang";
  return `
    <section class="card patient-card">
      <div class="patient-top">
        <div class="name-row">
          ${patientVisual(patient, "initial", "small")}
          <div>
            <h3>${patient.name}｜${patient.age}｜${patient.identity}</h3>
            <p class="complaint">主诉：${patient.complaint}</p>
          </div>
        </div>
        <span class="badge ${patient.difficulty === "入门" ? "green" : "apricot"}">${patient.difficulty}</span>
      </div>
      <div class="patient-meta">
        <div><span>患者状态</span><strong>${patient.status}</strong></div>
        <div><span>训练重点</span><strong>${patient.focus}</strong></div>
      </div>
      <div class="tag-row">${patient.tags.map(tag => `<span class="tag">${tag}</span>`).join("")}</div>
      <button class="primary-btn" ${disabled ? `data-toast="王先生胃脘案将在下一版开放。"` : `data-case="${patient.id}" data-route="patient"`}>进入诊室</button>
    </section>
  `;
}

function pageTitle(title, subtitle) {
  return `
    <section class="hero">
      <p class="eyebrow">${DATA.brand} · ${DATA.clinic.name}</p>
      <h1>${title}</h1>
      <p class="hero-copy">${subtitle}</p>
      <span class="level-pill">教学训练 · 虚拟模拟</span>
    </section>
  `;
}

function reviewBlock(title, items) {
  return `
    <section class="card">
      <h3>${title}</h3>
      <div class="summary-list">${items.map(item => `<div>${item}</div>`).join("")}</div>
    </section>
  `;
}

function patientVisual(patient, stage = "initial", size = "normal") {
  const profile = patient.patientProfile || {};
  const image = profile.images?.[stage] || profile.avatar || "";
  const icon = profile.icon || patient.icon || "人";
  return `
    <div class="patient-visual ${size}" style="--patient-img:url('${image}')">
      <div class="patient-visual-fallback">${icon}</div>
    </div>
  `;
}

function patientProfileCard(patient, stateText = "") {
  const profile = patient.patientProfile || {};
  return `
    <section class="card patient-profile-card">
      ${patientVisual(patient, "initial", "large")}
      <div>
        <h3>${patient.name}</h3>
        <p class="muted">${patient.age}｜${patient.identity}｜初诊</p>
        <span class="badge green">虚拟患者</span>
        <p class="complaint">今日状态：${stateText || profile.today || patient.status}</p>
      </div>
    </section>
  `;
}

function getPatientDetailView() {
  const patient = getActivePatient();
  return patient.detailView || {
    chief: `${patient.name}，${patient.age}，主诉：${patient.complaint}`,
    observable: patient.status || patient.patientProfile?.today || "",
    inquiryOpener: patient.description || patient.complaint,
    entryStatus: {
      patient: "初诊中",
      task: "收集辨证证据",
      reminder: "先把关键证据问清楚，再进入下一步判断。"
    },
    entryTips: {},
    evidencePlan: []
  };
}

function getActiveEvidencePlan() {
  return getPatientDetailView().evidencePlan || [];
}

function getEvidenceStatusEntries() {
  const unlockedEvidence = getCaseState().unlockedEvidence || {};
  return getActiveEvidencePlan().map(item => {
    const finding = unlockedEvidence[item.label];
    return {
      label: item.label,
      locked: item.locked || "未问",
      finding: finding || "",
      unlocked: Boolean(finding),
      countInProgress: item.countInProgress !== false
    };
  });
}

function getEvidenceProgress() {
  const items = getEvidenceStatusEntries().filter(item => item.countInProgress !== false);
  return {
    total: items.length,
    unlockedCount: items.filter(item => item.unlocked).length,
    items
  };
}

function getDiagnosisGuardMessage() {
  const progress = getEvidenceProgress();
  if (progress.unlockedCount >= 5) return "";
  const missing = getEvidenceStatusEntries()
    .filter(item => !item.unlocked && item.countInProgress !== false)
    .map(item => item.label);
  return `当前关键证据不足，建议至少获得 5 条证据后再进入辨证。你还缺少：${missing.join("、")}。`;
}

function renderPatientEvidenceStatus() {
  return getEvidenceStatusEntries().map(item => item.unlocked
    ? `
      <div class="evidence-status-item unlocked">
        <div class="evidence-status-head">
          <span class="evidence-status-label">${item.label}</span>
          <strong class="evidence-status-flag">✓ 已获得</strong>
        </div>
        <p class="evidence-status-finding">${item.finding}</p>
      </div>
    `
    : `
      <button type="button" class="evidence-status-item evidence-entry-btn ${state.activeEvidenceGuide === item.label ? "active" : ""}" data-evidence-entry="${item.label}">
        <div class="evidence-status-head">
          <span class="evidence-status-label">${item.label}</span>
          <strong class="evidence-status-flag"><span class="evidence-lock-icon" aria-hidden="true">🔒</span>${getEvidenceLockedStatusText(item.locked)}</strong>
        </div>
      </button>
    `
  ).join("");
}

function renderEvidenceGuidePanel() {
  const label = state.activeEvidenceGuide;
  if (!label) return "";
  const hint = findHintForEvidenceLabel(label);
  return `
    <section class="evidence-guide-panel">
      <div class="section-head compact evidence-guide-head">
        <div>
          <h3>${label}</h3>
          <p class="muted">先听带教提醒，再带着这个方向继续问诊。</p>
        </div>
        <button type="button" class="ghost-btn evidence-guide-close" data-close-evidence-guide>收起</button>
      </div>
      <p class="evidence-guide-copy">${getEntryTipForLabel(label)}</p>
      <div class="button-row evidence-guide-actions">
        <button type="button" class="soft-btn" data-evidence-ask="${label}">${hint?.actionLabel || "去问这个问题"}</button>
      </div>
    </section>
  `;
}

function renderEntryStatusCard() {
  const entryStatus = getPatientDetailView().entryStatus || {};
  return `
    <section class="card entry-status-card">
      <span class="badge green">带教状态</span>
      <div class="entry-status-list">
        <div><span>当前患者</span><strong>${entryStatus.patient || "初诊中"}</strong></div>
        <div><span>当前任务</span><strong>${entryStatus.task || "收集辨证证据"}</strong></div>
        <div><span>带教提醒</span><strong>${entryStatus.reminder || "先把关键证据问清楚，再进入下一步判断。"}</strong></div>
      </div>
    </section>
  `;
}

function getEntryTipForLabel(label) {
  return getPatientDetailView().entryTips?.[label] || "先围绕这条线索继续取证，再把它和其他证据合起来看。";
}

function getEvidenceLockedStatusText(status) {
  if (status === "未看") return "待观察";
  if (status === "未切") return "待切脉";
  return "待追问";
}

function findHintForEvidenceLabel(label) {
  return getActiveHintLadders().find(item => item.title === label || getEvidenceMetaByKey(item.key)?.category === label) || null;
}

function patientStateCard(patient, stage, label, statusText) {
  const profile = patient.patientProfile || {};
  const visualText = profile.visualState?.[stage] || profile.visualState?.initial || "";
  return `
    <section class="card patient-state-card">
      ${patientVisual(patient, stage, "large")}
      <div>
        <span class="badge green">${label}</span>
        <h3>${patient.name}状态变化</h3>
        <p class="complaint">状态：${statusText}</p>
        <p class="muted">形象提示：${visualText}</p>
      </div>
    </section>
  `;
}

function patientResultCard(patient, text, badge) {
  return `
    <section class="card result-card patient-result-card">
      ${patientVisual(patient, "recovered", "large")}
      <div>
        <span class="badge green">获得徽章：${badge}</span>
        <h3>${patient.name}第3天反馈</h3>
        <p class="complaint">${text}本案已收入你的小满医馆病案架。</p>
      </div>
    </section>
  `;
}

function renderColdStageProgress(current) {
  const stages = ["问诊取证", "辨证选方", "方药匹配", "模拟服药", "病案复盘"];
  const currentIndex = stages.indexOf(current);
  return `
    <section class="stage-strip">
      ${stages.map((stage, index) => `<span class="${index < currentIndex ? "done" : index === currentIndex ? "active" : ""}">${stage}</span>${index < stages.length - 1 ? "<b>→</b>" : ""}`).join("")}
    </section>
  `;
}

function renderColdDiagnosis() {
  const rawStep = state.coldLearningStep || 1;
  const step = rawStep === 4 && !(state.coldGroupSubmitted && isColdHerbGroupSelectionCorrect()) ? 3 : rawStep;
  return `
    <article class="page">
      ${renderColdStageProgress("辨证选方")}
      ${pageTitle(getColdLearningMeta(step).title, getColdLearningMeta(step).subtitle)}
      <div class="diagnosis-toolbar">
        <button class="ghost-btn" data-route="inquiry">返回问诊</button>
        <button class="soft-btn" data-toggle-case-summary>${state.showCaseSummary ? "收起病案" : "查看病案"}</button>
      </div>
      ${renderColdLearningStrip(step)}
      ${state.showCaseSummary ? renderCaseSummary() : ""}
      ${step === 1 ? renderColdSyndromeStage() : ""}
      ${step === 2 ? renderColdMethodFormulaStage() : ""}
      ${step === 3 ? renderColdHerbLearningStage() : ""}
      ${step === 4 ? renderColdHerbMeaningStage() : ""}
      <p class="notice">${DATA.disclaimer}</p>
    </article>
  `;
}

function getColdLearningMeta(step) {
  const meta = {
    1: {
      title: "第一关：判断证型",
      subtitle: "先不要急着选方，先根据已获得证据判断这属于哪一类感冒。"
    },
    2: {
      title: "第二关：定治法，选方剂",
      subtitle: "证型明确后，先确定治疗方向，再选择合适方剂。"
    },
    3: {
      title: "第三关A：判断药组",
      subtitle: "先判断本案需要哪些药组共同组成方义。"
    },
    4: {
      title: "第三关B：方义拆解",
      subtitle: "不要先背药名，先看它们在方中各自负责什么任务。"
    }
  };
  return meta[step] || meta[1];
}

function renderColdLearningStrip(step) {
  const steps = ["判断证型", "定治法与方剂", "判断药组", "方义拆解"];
  return `
    <section class="learning-strip">
      ${steps.map((label, index) => `<span class="${step === index + 1 ? "active" : step > index + 1 ? "done" : ""}">${index + 1}. ${label}</span>${index < steps.length - 1 ? "<b>→</b>" : ""}`).join("")}
    </section>
  `;
}

function renderColdEvidenceSnapshot() {
  const progress = getEvidenceProgress();
  const entries = getEvidenceStatusEntries();
  return `
    <section class="card evidence-review-card">
      <div class="section-head compact">
        <h2>已获得关键证据</h2>
        <span class="tiny-pill">${progress.unlockedCount}/7</span>
      </div>
      <div class="evidence-review-list">
        ${entries.map(item => `
          <div class="evidence-review-item ${item.unlocked ? "unlocked" : "locked"}">
            <strong>${item.label}</strong>
            <span>${item.unlocked ? item.finding : "未获得"}</span>
          </div>
        `).join("")}
      </div>
    </section>
  `;
}

function getColdSelected(group) {
  return state.coldSelections[group]?.selected?.[0] || "";
}

function isColdCorrectSyndrome() {
  return getColdSelected("证型判断") === DATA.coldAnswer.syndrome;
}

function isColdCorrectMethod() {
  return getColdSelected("治法选择") === DATA.coldAnswer.method;
}

function hasColdFormulaSelection() {
  return Boolean(getColdSelected("方剂选择"));
}

function isColdCorrectFormula() {
  return getColdSelected("方剂选择") === DATA.coldAnswer.formula;
}

function isColdHerbGroupSelectionCorrect(selectedGroups = state.coldGroupSelections || []) {
  const required = ["wind-cold", "lung-phlegm", "supportive"];
  return required.every(groupId => selectedGroups.includes(groupId))
    && !selectedGroups.includes("wind-heat")
    && !selectedGroups.includes("damp-yin");
}

function canEnterColdObservation() {
  return isColdCorrectSyndrome()
    && isColdCorrectMethod()
    && isColdCorrectFormula()
    && state.coldGroupSubmitted
    && isColdHerbGroupSelectionCorrect()
    && state.coldMeaningViewed;
}

function renderColdSyndromeStage() {
  const selected = getColdSelected("证型判断");
  const feedback = getColdSyndromeFeedback(selected);
  const isCorrect = selected === DATA.coldAnswer.syndrome;
  return `
    ${renderColdEvidenceSnapshot()}
    <section class="card learning-card">
      <div class="form-group">
        <div class="form-title"><span>证型判断</span><span class="muted">单选</span></div>
        <div class="learning-choice-list">
          ${state.coldSelections["证型判断"].options.map(option => `
            <button type="button" class="learning-choice-btn ${selected === option ? "selected" : ""}" data-cold-syndrome="${option}">
              ${option}
            </button>
          `).join("")}
        </div>
      </div>
      ${feedback ? `<div class="coach-tip standalone">${feedback}</div>` : ""}
      ${selected
        ? isCorrect
          ? `<button class="primary-btn full" data-cold-step="2">进入下一关</button>`
          : `<div class="button-row">
              <button class="ghost-btn" data-cold-reset-syndrome>重新判断</button>
              <button class="soft-btn" data-cold-standard-path>查看标准路径</button>
            </div>
            ${state.coldShowSyndromePathCard ? `
              <section class="coach-tip standalone standard-path-card">
                <strong>本案标准路径：</strong>
                <span class="path-sequence">风寒感冒 → 辛温解表，宣肺散寒 → 荆防败毒散加减</span>
                <p>这条路径不是让你背答案，而是让你看到：证据先汇聚成证型，证型再决定治法，治法再引出方剂。</p>
                <button class="primary-btn full" data-cold-follow-syndrome-path>沿标准路径继续</button>
              </section>
            ` : ""}`
        : `<button class="primary-btn full" disabled>请选择一个证型</button>`}
    </section>
  `;
}

function renderColdMethodFormulaStage() {
  const method = getColdSelected("治法选择");
  const formula = getColdSelected("方剂选择");
  const methodFeedback = getColdMethodFeedback(method);
  const formulaFeedback = getColdFormulaFeedback(formula);
  const methodCorrect = isColdCorrectMethod();
  const formulaCorrect = isColdCorrectFormula();
  return `
    <section class="card learning-card">
      <div class="form-group">
        <h3>既然判断为风寒感冒，治疗方向应该是什么？</h3>
        <div class="learning-choice-list">
          ${state.coldSelections["治法选择"].options.map(option => `
            <button type="button" class="learning-choice-btn ${method === option ? "selected" : ""}" data-cold-method="${option}">
              ${option}
            </button>
          `).join("")}
        </div>
      </div>
      ${methodFeedback ? `<div class="coach-tip standalone">${methodFeedback}</div>` : ""}
      ${method && !methodCorrect ? `<button class="ghost-btn full" data-cold-reset-method>重新选择治法</button>` : ""}
    </section>
    ${methodCorrect ? `
      <section class="card learning-card">
        <div class="form-group">
          <h3>在这个治法下，本案更适合哪个方剂方向？</h3>
          <div class="learning-choice-list">
            ${state.coldSelections["方剂选择"].options.map(option => `
              <button type="button" class="learning-choice-btn ${formula === option ? "selected" : ""}" data-cold-formula="${option}">
                ${option}
              </button>
            `).join("")}
          </div>
        </div>
        ${formulaFeedback ? `<div class="coach-tip standalone">${formulaFeedback}</div>` : ""}
        ${formula && !formulaCorrect ? `
          <div class="button-row">
            <button class="ghost-btn" data-cold-reset-formula>重新选择方剂</button>
            <button class="soft-btn" data-cold-show-formula-path>查看标准方剂路径</button>
          </div>
        ` : ""}
        ${state.coldShowFormulaPathCard ? `
          <section class="coach-tip standalone standard-path-card">
            <strong>本案标准路径：</strong>
            <span class="path-sequence">风寒感冒 → 辛温解表，宣肺散寒 → 荆防败毒散加减</span>
            <p>这条路径不是让你背答案，而是让你看到：证据先汇聚成证型，证型再决定治法，治法再引出方剂。</p>
            <button class="primary-btn full" data-cold-follow-formula-path>沿标准路径继续</button>
          </section>
        ` : ""}
      </section>
    ` : ""}
    <div class="button-row">
      <button class="ghost-btn" data-cold-step="1">返回上一关</button>
      <button class="primary-btn" data-cold-step="3" ${methodCorrect && formulaCorrect ? "" : "disabled"}>进入下一关：认识药味</button>
    </div>
  `;
}

function renderColdHerbLearningStage() {
  const groups = getColdHerbGroupConfigs();
  const selectedGroups = state.coldGroupSelections || [];
  const submitted = state.coldGroupSubmitted;
  const feedback = submitted ? getColdHerbGroupFeedback(selectedGroups) : "";
  const groupsCorrect = submitted && isColdHerbGroupSelectionCorrect(selectedGroups);
  return `
    <section class="card learning-card">
      <div class="form-group">
        <h3>本案需要哪些药组共同组成方义？可多选。</h3>
        <div class="group-judge-list">
          ${groups.map(group => `
            <button type="button" class="group-judge-card ${selectedGroups.includes(group.id) ? "selected" : ""}" data-cold-group="${group.id}">
              <div class="group-judge-head">
                <strong>${group.title}</strong>
                <span>${selectedGroups.includes(group.id) ? "已选择" : "点击判断"}</span>
              </div>
              <p>${group.herbs.join("、")}</p>
              <small>${group.summary}</small>
            </button>
          `).join("")}
        </div>
      </div>
      <div class="button-row">
        <button class="ghost-btn" data-cold-step="2">返回上一关</button>
        <button class="primary-btn" data-cold-submit-groups ${selectedGroups.length ? "" : "disabled"}>提交药组判断</button>
      </div>
      ${feedback ? `<div class="coach-tip standalone">${feedback}</div>` : ""}
      ${submitted && !groupsCorrect ? `<button class="ghost-btn full" data-cold-reset-groups>重新判断药组</button>` : ""}
    </section>
    ${groupsCorrect ? `
      <section class="card learning-card">
        <div class="section-head compact">
          <h2>这一关判断已经走顺了</h2>
          <span class="tiny-pill">药组结构已形成</span>
        </div>
        <p class="muted">本案药组结构是：</p>
        <div class="summary-list">
          <div><strong>主攻：</strong>风寒解表组</div>
          <div><strong>配合：</strong>宣肺化痰组</div>
          <div><strong>辅助：</strong>和中辅助组</div>
        </div>
        <p class="muted">接下来不要急着背药名，先看看每组药在方中负责什么任务。</p>
        <button class="primary-btn full" data-cold-open-meaning>查看方义拆解</button>
      </section>
    ` : ""}
  `;
}

function getColdSyndromeFeedback(option) {
  if (!option) return "";
  if (option === "风寒感冒") {
    return "这个判断方向对。本案以恶寒明显、发热较轻、无汗、清涕、白稀痰、苔薄白、脉浮紧为主要证据，整体更支持风寒束表。<br><br>这些证据不是单独决定结果，而是共同指向风寒。中医辨证看的是症状组合，不是一个症状对应一个证型。";
  }
  if (option === "风热感冒") {
    return "这个判断还需要再想一想。风热感冒通常更常见发热较重、咽喉红肿疼痛、口渴、黄涕、黄痰、苔薄黄、脉浮数。本案这些热象并不明显。";
  }
  return "这个判断方向不太符合。本案缺少暑湿困中的核心证据，比如身热不扬、头重如裹、胸闷泛恶、腹胀便溏、夏季暑湿环境等。";
}

function getColdMethodFeedback(option) {
  if (!option) return "";
  const feedbackMap = {
    "辛温解表，宣肺散寒": "这个方向对。寒邪在表，像把体表关住了，所以要用辛温之品把表打开，让寒邪从表而解；同时鼻塞、清涕、咳嗽提示肺气不宣，所以要宣肺散寒。",
    "辛凉解表，清宣肺热": "这个方向更适合风热感冒。本案热象不明显，若过早偏清热，可能没有解决外寒束表这个核心矛盾。",
    "清暑化湿，解表和中": "这个方向更适合暑湿感冒。本案缺少胸闷、泛恶、腹胀便溏、身热不扬等暑湿困中的证据。",
    "滋阴清热，润肺止咳": "这个方向更适合阴虚燥热一类问题。本案是外感初起，不是以阴虚内热为主。"
  };
  return feedbackMap[option] || "";
}

function getColdFormulaFeedback(option) {
  if (!option) return "";
  if (option === "荆防败毒散加减") {
    return "这个选择更合适。本案属于风寒表证，伴头痛、身痛、鼻塞清涕、白稀痰，适合以荆防败毒散加减，疏风散寒、解表宣肺。<br><br><strong>为什么不是其他方：</strong><ul class=\"feedback-list\"><li>不是银翘散：本案没有明显咽喉红肿疼痛、黄涕、黄痰、苔黄等风热证据。</li><li>不是藿香正气散：本案没有胸闷泛恶、腹胀便溏、暑湿困中等表现。</li><li>不是桂枝汤：桂枝汤更强调营卫不和、自汗出，本案以无汗、恶寒明显为主。</li><li>不是麻黄汤：麻黄汤偏表实无汗、恶寒发热、身痛而喘。本案有风寒表证，但没有明显喘咳重症，教学上先用荆防败毒散更稳。</li></ul>";
  }
  if (option === "麻黄汤加减") {
    return "这个方向和风寒表证有关系，但还不算本案里最稳的教学选择。麻黄汤更强调表实无汗、恶寒发热、身痛而喘，发散力量也更峻。本案虽然无汗、恶寒明显，但没有明显喘咳重症，先从荆防败毒散理解更合适。";
  }
  if (option === "桂枝汤加减") {
    return "这个方向和风寒也有关，但更偏营卫不和、自汗出。本案关键线索是无汗、恶寒明显，所以和桂枝汤主线不完全相合。";
  }
  if (option === "银翘散加减") {
    return "这里需要再想一想。银翘散更适合风热感冒，通常要看到发热较重、咽喉红肿疼痛、口渴、黄涕、黄痰等热象。本案这些证据都不够。";
  }
  return "这个方向更适合暑湿困中的路径。藿香正气散加减更看胸闷、泛恶、腹胀便溏、身重困倦这类证据，本案主线不在这里。";
}

function getColdHerbGroupConfigs() {
  return [
    {
      id: "wind-cold",
      title: "风寒解表组",
      herbs: ["荆芥", "防风", "羌活", "独活"],
      summary: "这一组负责把体表的风寒往外散，是本案的主力方向。",
      details: {
        荆芥: "帮助把在体表的风邪往外散，是解表常用药。",
        防风: "祛风解表，常和荆芥一起配合。",
        羌活: "偏走上半身和体表，适合头痛、身痛明显时。",
        独活: "也能祛风散寒，偏帮助身体疼痛、肢体不适。"
      }
    },
    {
      id: "lung-phlegm",
      title: "宣肺化痰组",
      herbs: ["前胡", "桔梗"],
      summary: "这一组帮助肺气宣通，缓解鼻塞、咳嗽、痰白。",
      details: {
        前胡: "帮助肺气往下走，适合咳嗽、痰不顺。",
        桔梗: "帮助肺气宣通，也像把药力往上带到肺和咽喉。"
      }
    },
    {
      id: "supportive",
      title: "和中辅助组",
      herbs: ["枳壳", "茯苓", "甘草", "生姜"],
      summary: "这一组帮助气机通畅、照顾脾胃，也让方子更平衡。",
      details: {
        枳壳: "帮助气机通畅，减少胸闷、气滞感。",
        茯苓: "帮助脾运化水湿，避免痰湿拖住病情。",
        甘草: "调和诸药，也能缓和咳嗽不适。",
        生姜: "温中散寒，帮助外寒从表而解，也照顾胃气。"
      }
    },
    {
      id: "wind-heat",
      title: "风热干扰组",
      herbs: ["金银花", "连翘", "石膏", "黄芩"],
      summary: "这一组偏清热，更适合风热或里热明显时。本案不是主力方向。",
      details: {
        金银花: "偏清热解毒，适合热象明显时。本案没有明显黄涕、黄痰、咽喉红肿疼痛。",
        连翘: "偏清热散结，更适合热象更鲜明时，本案不是主线。",
        石膏: "偏清气分实热，适合高热、口渴、大汗等热象明显时。本案不符合。",
        黄芩: "偏清热燥湿，更适合热象和湿热并见时，本案证据不足。"
      }
    },
    {
      id: "damp-yin",
      title: "暑湿/阴虚干扰组",
      herbs: ["藿香", "佩兰", "麦冬", "沙参"],
      summary: "这些药有各自适用场景，但本案缺少暑湿或阴虚的核心证据。",
      details: {
        藿香: "偏化湿和中，更适合胸闷、泛恶、腹胀这类暑湿困中表现。",
        佩兰: "偏芳香化湿，适合口中黏腻、湿困较重时。",
        麦冬: "偏养阴润燥，适合阴虚或燥咳。本案是外感风寒初起，不是阴虚为主。",
        沙参: "偏养阴清肺，适合燥咳、咽干明显时，本案不以这一条为主。"
      }
    }
  ];
}

function getColdHerbGroupFeedback(selectedGroups) {
  if (!selectedGroups.length) return "";
  const required = ["wind-cold", "lung-phlegm", "supportive"];
  const distractors = ["wind-heat", "damp-yin"];
  const pickedRequired = selectedGroups.filter(item => required.includes(item));
  const pickedDistractors = selectedGroups.filter(item => distractors.includes(item));
  if (pickedRequired.length === required.length && pickedDistractors.length === 0) {
    return "这个判断方向对。本案核心是风寒束表、肺气不宣，所以要以风寒解表组为主，配合宣肺化痰组；和中辅助组用来调畅气机、照顾中焦。风热清热药和暑湿、阴虚方向不是本案主线。";
  }
  if (pickedDistractors.length) {
    return "这里还需要再想一想。你选到的清热组或暑湿/阴虚组，更像其他路径会用到的力量。本案主线还是风寒束表、肺气不宣，先把主力药组抓稳。";
  }
  return "这里还需要再想一想。本案不只是散寒，还要兼顾肺气宣通和中焦调和，所以主力药组最好成串来看，而不是只抓住其中一部分。";
}

function getColdGroupHerbSelection(selectedGroups) {
  const map = Object.fromEntries(getColdHerbGroupConfigs().map(group => [group.id, group.herbs]));
  return [...new Set(selectedGroups.flatMap(groupId => map[groupId] || []))];
}

function getColdHerbStudyPanels() {
  const groups = getColdHerbGroupConfigs();
  const findGroup = id => groups.find(group => group.id === id);
  const windCold = findGroup("wind-cold");
  const lungPhlegm = findGroup("lung-phlegm");
  const supportive = findGroup("supportive");
  return [
    {
      id: "wind-cold",
      title: "主攻：风寒解表组",
      summary: "这一组负责把体表风寒往外散，是本案的主攻方向。",
      items: windCold.herbs.map(herb => ({ name: herb, text: windCold.details[herb] }))
    },
    {
      id: "lung-phlegm",
      title: "配合：宣肺化痰组",
      summary: "这一组帮助肺气宣通，缓解鼻塞、咳嗽、痰白。",
      items: lungPhlegm.herbs.map(herb => ({ name: herb, text: lungPhlegm.details[herb] }))
    },
    {
      id: "supportive",
      title: "辅助：和中辅助组",
      summary: "这一组调畅气机、照顾脾胃，让方子更平衡。",
      items: supportive.herbs.map(herb => ({ name: herb, text: supportive.details[herb] }))
    },
    {
      id: "distractor",
      title: "相邻方向辨析",
      summary: "清热、暑湿、养阴这些方向都有适用场景，但本案证据暂时不支持它们作为主线。",
      items: [
        { name: "清热方向：金银花、连翘、石膏、黄芩", text: "这些药偏清热，更适合风热或里热明显时。本案没有明显黄涕、黄痰、咽喉红肿疼痛、苔黄等热象，所以暂不作为主线。" },
        { name: "暑湿方向：藿香、佩兰", text: "这些药更适合暑湿困中，常见胸闷、泛恶、腹胀、身热不扬等。本案证据不突出。" },
        { name: "养阴方向：麦冬、沙参", text: "这些药偏养阴润燥，适合阴虚或燥咳。本案是外感风寒初起，不是阴虚为主。" }
      ]
    }
  ];
}

function renderColdHerbMeaningStage() {
  const studyPanels = getColdHerbStudyPanels();
  const observationReady = canEnterColdObservation() && state.coldMeaningViewed;
  return `
    <section class="card learning-card">
      <div class="section-head compact">
        <h2>方义拆解：每组药在做什么</h2>
        <span class="tiny-pill">先看任务，再记药味</span>
      </div>
      <p class="muted">不要先背药名，先看它们在方中各自负责什么任务。</p>
      ${studyPanels.map(panel => `
        <div class="herb-study-group ${state.coldExpandedHerbPanels.includes(panel.id) ? "expanded" : ""}">
          <button type="button" class="study-panel-toggle" data-cold-study-panel="${panel.id}">
            <div>
              <h3>${panel.title}</h3>
              <p class="muted">${panel.summary}</p>
            </div>
            <span>${state.coldExpandedHerbPanels.includes(panel.id) ? "收起" : "展开"}</span>
          </button>
          ${state.coldExpandedHerbPanels.includes(panel.id) ? `
            <div class="herb-study-list">
              ${panel.items.map(item => `
                <div class="herb-study-item">
                  <strong>${item.name}</strong>
                  <p>${item.text}</p>
                </div>
              `).join("")}
            </div>
          ` : ""}
        </div>
      `).join("")}
    </section>
    <section class="card learning-card">
      <div class="button-row">
        <button class="ghost-btn" data-cold-step="3">返回药组判断</button>
        <button class="primary-btn" data-route="observation" ${observationReady ? "" : "disabled"}>进入模拟服药观察</button>
      </div>
      ${observationReady ? "" : `<p class="muted">请先完成药组判断，并进入方义拆解后，再进入模拟服药观察。</p>`}
    </section>
  `;
}

function resetColdLearningFrom(stage) {
  if (stage <= 2) {
    state.coldShowSyndromePathCard = false;
    state.coldSelections["治法选择"].selected = [];
    state.coldSelections["方剂选择"].selected = [];
  }
  if (stage <= 3) {
    state.coldGroupSelections = [];
    state.coldGroupSubmitted = false;
    state.coldExpandedHerbPanels = [];
    state.coldShowFormulaPathCard = false;
    state.coldMeaningViewed = false;
    state.coldSelections["中药配方选择"].selected = [];
    state.coldSelections["中成药选择"].selected = [];
  }
}

function renderColdOptionLabel(group, option) {
  if (group !== "中药配方选择") return option;
  const fn = DATA.coldAnswer.herbFunctions[option] || "";
  return `<span class="herb-name">${option}</span>${fn ? `<small>${fn}</small>` : ""}`;
}

function renderHerbGroups(herbConfig) {
  const groups = [
    ["风寒解表组", ["荆芥", "防风", "羌活", "独活"]],
    ["宣肺化痰组", ["柴胡", "前胡", "桔梗", "枳壳"]],
    ["和中辅助组", ["茯苓", "川芎", "甘草", "生姜"]],
    ["风热干扰组", ["金银花", "连翘", "薄荷", "牛蒡子", "黄芩", "石膏"]],
    ["暑湿/阴虚干扰组", ["香薷", "藿香", "佩兰", "麦冬", "沙参"]]
  ];
  return groups.map(([title, herbs]) => `
    <div class="herb-group">
      <h4>${title}</h4>
      <div class="chip-grid medicine-pool">
        ${herbs.map(option => `<button class="chip ${herbConfig.selected.includes(option) ? "selected" : ""}" data-group="中药配方选择" data-option="${option}">${renderColdOptionLabel("中药配方选择", option)}</button>`).join("")}
      </div>
    </div>
  `).join("");
}

function renderColdDosage() {
  const herbs = state.coldSelections["中药配方选择"].selected;
  return `
    <article class="page">
      ${renderColdStageProgress("方药匹配")}
      ${pageTitle("参考剂量确认", "展示你已选药物及系统教学参考剂量。")}
      <section class="card"><p class="complaint">入门级病例重点考察方药组成匹配，剂量为系统教学参考。</p></section>
      <section class="card">
        <h3>已选中药组成</h3>
        ${herbs.length ? `
          <div class="dosage-list">
            ${herbs.map(herb => {
              const warning = getHerbWarning(herb);
              return `
                <div class="dosage-item ${warning ? "warn" : ""}">
                  <div><strong>${herb}</strong><span>${DATA.coldAnswer.herbDosage[herb] || "教学参考"}</span></div>
                  ${warning ? `<p>${warning}</p>` : `<p>与本案方药方向较匹配。</p>`}
                </div>
              `;
            }).join("")}
          </div>
        ` : `<div class="empty-record">你还没有选择药物，请返回调整药味。</div>`}
      </section>
      <p class="notice">本页面剂量仅用于中医教学训练和方药组成理解，不作为真实患者用药建议。</p>
      <div class="button-row">
        <button class="ghost-btn" data-route="diagnosis">返回调整药味</button>
        <button class="primary-btn" data-confirm-dosage ${herbs.length ? "" : "disabled"}>确认配方，继续选择中成药</button>
      </div>
    </article>
  `;
}

function getHerbWarning(herb) {
  const windHeat = ["金银花", "连翘", "薄荷", "牛蒡子", "黄芩"];
  const realHeat = ["石膏"];
  const yin = ["麦冬", "沙参"];
  const damp = ["香薷", "藿香", "佩兰"];
  if (windHeat.includes(herb)) return "此药更偏风热/清热方向，本案不是首选。";
  if (realHeat.includes(herb)) return "此药更偏实热方向，本案不是首选。";
  if (yin.includes(herb)) return "此药更偏阴虚燥热方向，本案证据不足。";
  if (damp.includes(herb)) return "此药更偏暑湿/化湿方向，本案证据不足。";
  return "";
}

function formatHerbsWithDosage(herbs) {
  return herbs.map(herb => `${herb}${DATA.coldAnswer.herbDosage[herb] ? ` ${DATA.coldAnswer.herbDosage[herb]}` : ""}`).join("、");
}

function setActiveCase(caseId) {
  state.activeCaseId = caseId;
  if (!state.caseStates[caseId]) state.caseStates[caseId] = createCaseState();
  ensureCaseStateShape(state.caseStates[caseId]);
  if (state.savedCases[caseId] === undefined) state.savedCases[caseId] = false;
  state.showCaseSummary = false;
  state.showAiPrompts = false;
  state.showInfoPanel = false;
  state.showPendingInfo = false;
  state.activeEvidenceGuide = "";
  state.draftInquiryQuestion = "";
  state.coldLearningStep = 1;
  state.coldGroupSelections = [];
  state.coldGroupSubmitted = false;
  state.coldExpandedHerbPanels = [];
  state.coldShowSyndromePathCard = false;
  state.coldShowFormulaPathCard = false;
  state.coldMeaningViewed = false;
}

function isColdCase() {
  return state.activeCaseId === "zhou";
}

function getActivePatient() {
  return DATA.patients.find(patient => patient.id === state.activeCaseId) || DATA.patients[0];
}

function getCaseState() {
  if (!state.caseStates[state.activeCaseId]) state.caseStates[state.activeCaseId] = createCaseState();
  ensureCaseStateShape(state.caseStates[state.activeCaseId]);
  return state.caseStates[state.activeCaseId];
}

function getActiveQuestions() {
  return getActivePatient().questions || DATA.questions;
}

function getActiveKnowledgeBase() {
  return getActivePatient().knowledgeBase || DATA.patientKnowledgeBase;
}

function getMasteredInfoItems() {
  const unlocked = getCaseState().unlockedEvidence || {};
  const orderedLabels = getActiveEvidencePlan().map(item => item.label);
  const ordered = orderedLabels
    .filter(label => unlocked[label])
    .map(label => `${label}：${unlocked[label]}`);
  const extras = Object.entries(unlocked)
    .filter(([label]) => !orderedLabels.includes(label))
    .map(([, finding]) => `补充线索：${finding}`);
  return [...ordered, ...extras];
}

function getPendingInfoItems() {
  return getEvidenceStatusEntries()
    .filter(item => !item.unlocked)
    .map(item => `${item.label}：${item.locked}`);
}

function getActiveHintLadders() {
  return getActivePatient().hintLadders || [];
}

function getHintStage(hintId) {
  return getCaseState().hintProgress?.[hintId] || 0;
}

function hasUnlockedEvidenceCategory(key) {
  const meta = getEvidenceMetaByKey(key);
  return Boolean(meta?.category && getCaseState().unlockedEvidence?.[meta.category]);
}

function getEvidenceMetaByKey(key) {
  return getActivePatient().evidenceUnlocks?.[key] || null;
}

function createCaseState() {
  return {
    inquiryRecords: [],
    unlockedInfo: [],
    unlockedEvidence: {},
    hintProgress: {},
    nextRecordId: 1
  };
}

function ensureCaseStateShape(caseState) {
  caseState.inquiryRecords = caseState.inquiryRecords || [];
  caseState.unlockedInfo = caseState.unlockedInfo || [];
  caseState.unlockedEvidence = caseState.unlockedEvidence || {};
  caseState.hintProgress = caseState.hintProgress || {};
  caseState.nextRecordId = caseState.nextRecordId || 1;
}

function getColdInquiryAnalysis() {
  const records = getCaseState().inquiryRecords;
  const reviewKeys = ["怕冷", "发热", "汗", "鼻涕", "咽喉", "咽痛", "痰", "头痛", "身痛", "口渴", "胃肠", "舌象", "脉象"];
  const hitKeys = [...new Set(records
    .map(record => record.matchedKey === "清涕" ? "鼻涕" : record.matchedKey)
    .filter(key => reviewKeys.includes(key)))];
  const weakCount = records.filter(record => record.relevance === "low" || record.matchedKey === "未匹配").length;
  const keyHitCount = hitKeys.length;
  let completenessScore = keyHitCount <= 1 ? 52 : keyHitCount <= 3 ? 68 + keyHitCount * 2 : Math.min(92, 78 + (keyHitCount - 4) * 3);
  if (hitKeys.includes("汗") && hitKeys.includes("鼻涕")) completenessScore += 3;
  completenessScore = clamp(completenessScore - weakCount * 4, 45, 94);
  const evidenceBonus = (hitKeys.includes("汗") ? 4 : 0) + (hitKeys.includes("鼻涕") ? 4 : 0) + (hitKeys.includes("怕冷") ? 3 : 0) + Math.min(8, keyHitCount) - weakCount * 3;
  const missingKeys = reviewKeys.filter(key => !hitKeys.includes(key));
  const usefulText = hitKeys.length ? `你追问了“${hitKeys.join("、")}”，有助于区分风寒、风热和暑湿感冒。` : "你还没有命中感冒辨证的关键追问，建议先围绕怕冷发热、汗出、鼻涕、咽喉和痰色展开。";
  const missingText = missingKeys.length ? `本案还建议追问“${missingKeys.slice(0, 4).join("、")}”，以便确认风寒证据并排除风热、暑湿。` : "本案感冒鉴别追问覆盖较完整，可以进入证型与方药判断。";
  const weakText = weakCount ? "你追问的部分问题与本案外感感冒鉴别关系较弱。建议优先围绕寒热、汗出、鼻涕、咽痛、痰色和胃肠症状展开。" : "本轮追问基本围绕外感感冒辨证展开，问题方向较集中。";
  return {
    total: records.length,
    hitKeys,
    keyHitCount,
    weakCount,
    completenessScore,
    evidenceBonus,
    pointComment: usefulText,
    missingComment: missingText,
    weakComment: weakText,
    nextAdvice: missingKeys.length ? `下次建议补充追问${missingKeys.slice(0, 3).join("、")}，帮助完成风寒、风热、暑湿鉴别。` : "下次可继续追问症状变化与服药后反馈，形成完整感冒病案。"
  };
}

function renderInquiryConversation() {
  const p = getActivePatient();
  const opener = getPatientDetailView().inquiryOpener || p.description || p.complaint;
  return `
    <div class="conversation-flow">
      <div class="bubble-row patient">
        <div class="bubble light"><span>${p.name}</span>${escapeHTML(opener)}</div>
      </div>
      ${renderInquiryRecords(true)}
      ${getCaseState().inquiryRecords.length ? "" : `<div class="empty-record chat-empty">你可以先自己追问。问出关键证据后，系统会先请你判断它更支持什么，再给浅显解释。</div>`}
    </div>
  `;
}

function renderInquiryRecords(interactive = true) {
  const records = getCaseState().inquiryRecords;
  const patientName = getActivePatient().name;
  if (!records.length) {
    return "";
  }
  return `
    <div class="chat-records">
      ${records.map(record => `
        <div class="chat-pair">
          <div class="bubble-row student">
            <div class="bubble dark"><span>你</span>${escapeHTML(record.question)}</div>
          </div>
          ${record.speaker === "system"
            ? `<div class="coach-tip observation-tip"><strong>系统观察</strong>${escapeHTML(record.answer)}</div>`
            : `<div class="bubble-row patient"><div class="bubble light"><span>${patientName}</span>${escapeHTML(record.answer)}</div></div>`}
          ${record.unlockedFinding ? `<div class="evidence-chip-row"><span class="evidence-chip">${escapeHTML(getUnlockedChipLabel(record))}</span></div>` : ""}
          ${interactive && record.judgement && !record.judgement.choice ? renderEvidenceJudgeCard(record) : ""}
          ${record.coachTip ? `<div class="coach-tip">${escapeHTML(record.coachTip)}</div>` : ""}
        </div>
      `).join("")}
    </div>
  `;
}

function getUnlockedChipLabel(record) {
  if (!record?.unlockedFinding) return "";
  const orderedLabels = getActiveEvidencePlan().map(item => item.label);
  if (orderedLabels.includes(record.unlockedInfo)) return `已解锁证据：${record.unlockedFinding}`;
  return `补充线索：${record.unlockedFinding}`;
}

function renderEvidenceJudgeCard(record) {
  return `
    <section class="evidence-judge-card">
      <p>${record.judgement.prompt}</p>
      <div class="judge-option-grid">
        ${record.judgement.options.map(option => `
          <button class="ghost-btn judge-option-btn" data-judge-record="${record.id}" data-judge-option="${option.id}">
            <span>${option.id}.</span>${option.label}
          </button>
        `).join("")}
      </div>
    </section>
  `;
}

function renderDiagnosisReadyCard(progress) {
  if (progress.unlockedCount < 5) return "";
  return `
    <section class="ready-card">
      <span class="badge green">证据已形成</span>
      <h3>证据已基本足够，可以进入辨证。</h3>
      <p>你已经问出了 ${progress.unlockedCount} 条关键证据。接下来更重要的是，把这些证据合起来看，而不是只盯住某一条。</p>
      <button class="primary-btn" data-route="diagnosis">开始辨证</button>
    </section>
  `;
}

function renderAiPromptDrawer() {
  const hints = getActiveHintLadders();
  return `
    <div class="sheet-mask" data-close-sheet>
      <section class="bottom-sheet" aria-label="AI建议优先追问">
        <div class="sheet-handle"></div>
        <div class="sheet-head">
          <div>
            <h2>AI建议优先追问</h2>
            <p class="muted">先自己想，再来这里补漏。系统会先给方向，再给维度，最后才给具体问法。</p>
          </div>
          <button class="ghost-btn" data-toggle-ai-prompts>关闭</button>
        </div>
        <div class="recommend-list">
          ${hints.map(item => {
            const asked = hasAskedQuestion(item.actionQuestion, item.key) || hasUnlockedEvidenceCategory(item.key);
            const step = getHintStage(item.id);
            return `
              <div class="recommend-item hint-item ${asked ? "asked" : ""}">
                <div class="hint-item-main">
                  <strong>${item.title}</strong>
                  <div class="hint-step-list">
                    ${step >= 1 ? `<p>${item.stages[0]}</p>` : ""}
                    ${step >= 2 ? `<p>${item.stages[1]}</p>` : ""}
                    ${step >= 3 ? `<p class="hint-final">${item.stages[2]}</p>` : ""}
                  </div>
                </div>
                <div class="hint-item-actions">
                  ${asked
                    ? `<span class="tiny-pill">已补到</span>`
                    : step < 3
                      ? `<button class="ghost-btn" data-hint-reveal="${item.id}">${step === 0 ? "给我一点提示" : step === 1 ? "继续启发" : "再具体一点"}</button>`
                      : `<button class="soft-btn" data-hint-ask="${item.id}">${item.actionLabel || "按这个追问"}</button>`}
                </div>
              </div>
            `;
          }).join("")}
        </div>
        <div class="sheet-actions">
          <button class="ghost-btn full" data-toggle-ai-prompts>收起建议</button>
        </div>
      </section>
    </div>
  `;
}

function renderInquiryInfoPanel() {
  const masteredInfo = getMasteredInfoItems();
  const pendingInfo = getPendingInfoItems();
  return `
    <div class="sheet-mask info-mask" data-close-info>
      <section class="info-panel" aria-label="已获得证据">
        <div class="section-head compact">
          <h2>已获得证据</h2>
          <button class="ghost-btn" data-toggle-info-panel>关闭</button>
        </div>
        <div class="info-panel-block">
          <h3>已获得关键证据</h3>
          ${masteredInfo.length
            ? `<div class="tag-row">${masteredInfo.map(item => `<span class="tag soft-green">${item}</span>`).join("")}</div>`
            : `<p class="muted">你还没有问出关键证据。先围绕主诉、寒热、汗出和症状性质继续追问。</p>`}
        </div>
        <div class="info-panel-toggle">
          <button class="ghost-btn full" data-toggle-pending-info>${state.showPendingInfo ? "收起待完善信息" : "查看待完善信息"}</button>
        </div>
        ${state.showPendingInfo ? `
          <div class="info-panel-block">
            <h3>待完善信息</h3>
            <div class="tag-row">${pendingInfo.map(item => `<span class="tag soft-rice">${item}</span>`).join("")}</div>
          </div>
        ` : ""}
      </section>
    </div>
  `;
}

function renderCaseSummary() {
  const p = getActivePatient();
  const unlocked = getCaseState().unlockedEvidence || {};
  const masteredInfo = getMasteredInfoItems();
  const tongue = unlocked["舌象"] || "未看";
  const pulse = unlocked["脉象"] || "未切";
  return `
    <section class="card case-summary">
      <div class="section-head compact">
        <h2>本次病案摘要</h2>
        <span class="tiny-pill">虚拟病人</span>
      </div>
      <div class="summary-list">
        <div>${p.name}｜${p.age}｜${p.identity}</div>
        <div>主诉：${p.complaint}</div>
        <div>舌象：${tongue}</div>
        <div>脉象：${pulse}</div>
      </div>
      <h3>当前已获得证据</h3>
      ${masteredInfo.length
        ? `<div class="summary-list">${masteredInfo.map(item => `<div>${item}</div>`).join("")}</div>`
        : `<p class="muted">你还没有解锁关键证据。先围绕寒热、汗出、鼻涕、痰色、咽喉、舌象和脉象继续问诊。</p>`}
      <h3>已纳入问诊记录</h3>
      ${renderInquiryRecords(false) || `<p class="muted">你还没有开始问诊记录。</p>`}
    </section>
  `;
}

function bindPageActions() {
  app.querySelectorAll("[data-route]").forEach(el => {
    el.addEventListener("click", () => {
      if (el.dataset.route === "diagnosis" && state.page === "patient" && getEvidenceProgress().unlockedCount < 5) {
        showToast(getDiagnosisGuardMessage());
        return;
      }
      state.draftInquiryQuestion = "";
      if (el.dataset.case) setActiveCase(el.dataset.case);
      if (el.dataset.route === "diagnosis" && isColdCase() && state.page !== "diagnosis") {
        state.coldLearningStep = 1;
      }
      showStageToast(el.dataset.route);
      render(el.dataset.route);
    });
  });

  app.querySelectorAll("[data-toast]").forEach(el => {
    el.addEventListener("click", () => showToast(el.dataset.toast));
  });

  app.querySelectorAll("[data-evidence-entry]").forEach(button => {
    button.addEventListener("click", () => {
      state.activeEvidenceGuide = button.dataset.evidenceEntry || "";
      render("patient", { preserveScroll: true, noPageMotion: true });
    });
  });

  app.querySelectorAll("[data-close-evidence-guide]").forEach(button => {
    button.addEventListener("click", () => {
      state.activeEvidenceGuide = "";
      render("patient", { preserveScroll: true, noPageMotion: true });
    });
  });

  app.querySelectorAll("[data-evidence-ask]").forEach(button => {
    button.addEventListener("click", () => {
      const label = button.dataset.evidenceAsk || "";
      const hint = findHintForEvidenceLabel(label);
      state.draftInquiryQuestion = hint?.actionQuestion || `${label}怎样？`;
      state.activeEvidenceGuide = "";
      render("inquiry", { noPageMotion: true });
    });
  });

  app.querySelectorAll("[data-guard-diagnosis]").forEach(button => {
    button.addEventListener("click", () => {
      showToast(getDiagnosisGuardMessage());
    });
  });

  app.querySelectorAll("[data-hint-reveal]").forEach(button => {
    button.addEventListener("click", () => {
      const hintId = button.dataset.hintReveal;
      const caseState = getCaseState();
      caseState.hintProgress[hintId] = Math.min(3, (caseState.hintProgress[hintId] || 0) + 1);
      render("inquiry", { preserveScroll: true, noPageMotion: true });
    });
  });

  app.querySelectorAll("[data-hint-ask]").forEach(button => {
    button.addEventListener("click", () => {
      const hint = getActiveHintLadders().find(item => item.id === button.dataset.hintAsk);
      if (!hint) return;
      if (hasAskedQuestion(hint.actionQuestion, hint.key) || hasUnlockedEvidenceCategory(hint.key)) {
        showToast("这个方向你已经补到过了。");
        return;
      }
      const unlockedInfo = addInquiryRecord(hint.actionQuestion, matchPatientAnswer(hint.actionQuestion, hint.key), "hint");
      if (unlockedInfo?.label) state.pendingUnlockCue = { label: unlockedInfo.label };
      state.showAiPrompts = false;
      state.draftInquiryQuestion = "";
      render("inquiry", { scrollInquiryBottom: true, noPageMotion: true });
    });
  });

  app.querySelectorAll("[data-send-inquiry]").forEach(button => {
    button.addEventListener("click", () => {
      const input = document.querySelector("#freeInquiryInput");
      const question = input?.value.trim() || "";
      if (!question) {
        showToast("请先输入你想追问的问题。");
        return;
      }
      state.draftInquiryQuestion = "";
      const unlockedInfo = addInquiryRecord(question, matchPatientAnswer(question), "free");
      if (unlockedInfo?.label) state.pendingUnlockCue = { label: unlockedInfo.label };
      render("inquiry", { scrollInquiryBottom: true, noPageMotion: true });
    });
  });

  app.querySelectorAll("#freeInquiryInput").forEach(input => {
    input.addEventListener("keydown", event => {
      if (event.key !== "Enter") return;
      event.preventDefault();
      app.querySelector("[data-send-inquiry]")?.click();
    });
  });

  app.querySelectorAll("[data-voice-inquiry]").forEach(button => {
    button.addEventListener("click", () => {
      showToast("Demo版本暂用文字追问，正式版支持语音问诊训练。");
    });
  });

  app.querySelectorAll("[data-toggle-ai-prompts]").forEach(button => {
    button.addEventListener("click", () => {
      state.showAiPrompts = !state.showAiPrompts;
      if (state.showAiPrompts) state.showInfoPanel = false;
      render("inquiry", { preserveScroll: true, noPageMotion: true });
    });
  });

  app.querySelectorAll("[data-toggle-info-panel]").forEach(button => {
    button.addEventListener("click", () => {
      state.showInfoPanel = !state.showInfoPanel;
      if (state.showInfoPanel) state.showAiPrompts = false;
      if (!state.showInfoPanel) state.showPendingInfo = false;
      render("inquiry", { preserveScroll: true, noPageMotion: true });
    });
  });

  app.querySelectorAll("[data-toggle-pending-info]").forEach(button => {
    button.addEventListener("click", () => {
      state.showPendingInfo = !state.showPendingInfo;
      render("inquiry", { preserveScroll: true, noPageMotion: true });
    });
  });

  app.querySelectorAll("[data-close-sheet]").forEach(mask => {
    mask.addEventListener("click", event => {
      if (event.target !== mask) return;
      state.showAiPrompts = false;
      render("inquiry", { preserveScroll: true, noPageMotion: true });
    });
  });

  app.querySelectorAll("[data-close-info]").forEach(mask => {
    mask.addEventListener("click", event => {
      if (event.target !== mask) return;
      state.showInfoPanel = false;
      state.showPendingInfo = false;
      render("inquiry", { preserveScroll: true, noPageMotion: true });
    });
  });

  app.querySelectorAll("[data-judge-record]").forEach(button => {
    button.addEventListener("click", () => {
      const record = getCaseState().inquiryRecords.find(item => item.id === button.dataset.judgeRecord);
      if (!record?.judgement || record.judgement.choice) return;
      const choice = button.dataset.judgeOption;
      record.judgement.choice = choice;
      record.coachTip = record.judgement.feedback?.[choice] || "";
      render("inquiry", { scrollInquiryBottom: true, noPageMotion: true });
    });
  });

  app.querySelectorAll("[data-toggle-case-summary]").forEach(button => {
    button.addEventListener("click", () => {
      state.showCaseSummary = !state.showCaseSummary;
      render("diagnosis", { preserveScroll: true });
    });
  });

  app.querySelectorAll("[data-cold-step]").forEach(button => {
    button.addEventListener("click", () => {
      if (button.disabled) return;
      state.coldLearningStep = Number(button.dataset.coldStep) || 1;
      render("diagnosis");
    });
  });

  app.querySelectorAll("[data-cold-syndrome]").forEach(button => {
    button.addEventListener("click", () => {
      const option = button.dataset.coldSyndrome;
      if (!option) return;
      const current = getColdSelected("证型判断");
      state.coldSelections["证型判断"].selected = [option];
      if (current !== option) resetColdLearningFrom(2);
      render("diagnosis", { preserveScroll: true, noPageMotion: true });
    });
  });

  app.querySelectorAll("[data-cold-reset-syndrome]").forEach(button => {
    button.addEventListener("click", () => {
      state.coldSelections["证型判断"].selected = [];
      state.coldShowSyndromePathCard = false;
      resetColdLearningFrom(2);
      render("diagnosis", { preserveScroll: true, noPageMotion: true });
    });
  });

  app.querySelectorAll("[data-cold-standard-path]").forEach(button => {
    button.addEventListener("click", () => {
      state.coldShowSyndromePathCard = true;
      render("diagnosis", { preserveScroll: true, noPageMotion: true });
    });
  });

  app.querySelectorAll("[data-cold-follow-syndrome-path]").forEach(button => {
    button.addEventListener("click", () => {
      state.coldSelections["证型判断"].selected = [DATA.coldAnswer.syndrome];
      state.coldShowSyndromePathCard = false;
      resetColdLearningFrom(2);
      state.coldLearningStep = 2;
      render("diagnosis");
    });
  });

  app.querySelectorAll("[data-cold-method]").forEach(button => {
    button.addEventListener("click", () => {
      const option = button.dataset.coldMethod;
      if (!option) return;
      const current = getColdSelected("治法选择");
      state.coldSelections["治法选择"].selected = [option];
      state.coldShowFormulaPathCard = false;
      if (current !== option) resetColdLearningFrom(3);
      render("diagnosis", { preserveScroll: true, noPageMotion: true });
    });
  });

  app.querySelectorAll("[data-cold-reset-method]").forEach(button => {
    button.addEventListener("click", () => {
      state.coldSelections["治法选择"].selected = [];
      state.coldShowFormulaPathCard = false;
      resetColdLearningFrom(3);
      render("diagnosis", { preserveScroll: true, noPageMotion: true });
    });
  });

  app.querySelectorAll("[data-cold-formula]").forEach(button => {
    button.addEventListener("click", () => {
      const option = button.dataset.coldFormula;
      if (!option) return;
      const current = getColdSelected("方剂选择");
      state.coldSelections["方剂选择"].selected = [option];
      state.coldShowFormulaPathCard = false;
      if (current !== option) resetColdLearningFrom(3);
      render("diagnosis", { preserveScroll: true, noPageMotion: true });
    });
  });

  app.querySelectorAll("[data-cold-reset-formula]").forEach(button => {
    button.addEventListener("click", () => {
      state.coldSelections["方剂选择"].selected = [];
      state.coldShowFormulaPathCard = false;
      resetColdLearningFrom(3);
      render("diagnosis", { preserveScroll: true, noPageMotion: true });
    });
  });

  app.querySelectorAll("[data-cold-show-formula-path]").forEach(button => {
    button.addEventListener("click", () => {
      state.coldShowFormulaPathCard = true;
      render("diagnosis", { preserveScroll: true, noPageMotion: true });
    });
  });

  app.querySelectorAll("[data-cold-follow-formula-path]").forEach(button => {
    button.addEventListener("click", () => {
      state.coldSelections["方剂选择"].selected = [DATA.coldAnswer.formula];
      state.coldShowFormulaPathCard = false;
      resetColdLearningFrom(3);
      state.coldLearningStep = 3;
      render("diagnosis");
    });
  });

  app.querySelectorAll("[data-cold-group]").forEach(button => {
    button.addEventListener("click", () => {
      const groupId = button.dataset.coldGroup;
      if (!groupId) return;
      state.coldGroupSelections = state.coldGroupSelections.includes(groupId)
        ? state.coldGroupSelections.filter(item => item !== groupId)
        : [...state.coldGroupSelections, groupId];
      state.coldGroupSubmitted = false;
      state.coldExpandedHerbPanels = [];
      state.coldMeaningViewed = false;
      state.coldSelections["中药配方选择"].selected = getColdGroupHerbSelection(state.coldGroupSelections);
      render("diagnosis", { preserveScroll: true, noPageMotion: true });
    });
  });

  app.querySelectorAll("[data-cold-submit-groups]").forEach(button => {
    button.addEventListener("click", () => {
      if (!state.coldGroupSelections.length) {
        showToast("请先判断本案要依靠哪些药组。");
        return;
      }
      state.coldGroupSubmitted = true;
      state.coldExpandedHerbPanels = [];
      state.coldMeaningViewed = false;
      render("diagnosis", { preserveScroll: true, noPageMotion: true });
    });
  });

  app.querySelectorAll("[data-cold-reset-groups]").forEach(button => {
    button.addEventListener("click", () => {
      state.coldGroupSelections = [];
      state.coldGroupSubmitted = false;
      state.coldExpandedHerbPanels = [];
      state.coldMeaningViewed = false;
      state.coldSelections["中药配方选择"].selected = [];
      render("diagnosis", { preserveScroll: true, noPageMotion: true });
    });
  });

  app.querySelectorAll("[data-cold-open-meaning]").forEach(button => {
    button.addEventListener("click", () => {
      if (!(state.coldGroupSubmitted && isColdHerbGroupSelectionCorrect())) return;
      state.coldLearningStep = 4;
      state.coldMeaningViewed = true;
      state.coldExpandedHerbPanels = ["wind-cold"];
      render("diagnosis");
    });
  });

  app.querySelectorAll("[data-cold-study-panel]").forEach(button => {
    button.addEventListener("click", () => {
      const panelId = button.dataset.coldStudyPanel;
      if (!panelId) return;
      state.coldExpandedHerbPanels = state.coldExpandedHerbPanels.includes(panelId)
        ? state.coldExpandedHerbPanels.filter(item => item !== panelId)
        : [...state.coldExpandedHerbPanels, panelId];
      render("diagnosis", { preserveScroll: true, noPageMotion: true });
    });
  });

  app.querySelectorAll("[data-confirm-dosage]").forEach(button => {
    button.addEventListener("click", () => {
      if (!state.coldSelections["中药配方选择"].selected.length) {
        showToast("请先选择中药组成。");
        return;
      }
      state.coldDoseConfirmed = true;
      render("diagnosis");
    });
  });

  app.querySelectorAll(".chip").forEach(chip => {
    chip.addEventListener("click", () => {
      const selectionSet = isColdCase() ? state.coldSelections : state.selections;
      const group = selectionSet[chip.dataset.group];
      const option = chip.dataset.option;
      if (isColdCase() && chip.dataset.group === "中药配方选择") state.coldDoseConfirmed = false;
      if (isColdCase() && ["证型判断", "治法选择", "方剂选择"].includes(chip.dataset.group)) {
        state.coldChoiceTip = DATA.coldAnswer.choiceTips[option] || "";
      }
      if (isColdCase() && chip.dataset.group === "中药配方选择") {
        state.coldHerbTip = DATA.coldAnswer.herbTips[option] || `${option}：请结合本案风寒证据判断是否适合。`;
      }
      if (isColdCase() && chip.dataset.group === "中成药选择") {
        state.coldPatentTip = DATA.coldAnswer.patentTips[option] || "";
      }
      if (group.multi) {
        group.selected = group.selected.includes(option)
          ? group.selected.filter(item => item !== option)
          : [...group.selected, option];
      } else {
        group.selected = [option];
      }
      render("diagnosis", { preserveScroll: true });
    });
  });

  app.querySelectorAll("[data-expert]").forEach(button => {
    button.addEventListener("click", () => {
      state.activeExpert = button.dataset.expert;
      render("consult");
      setTimeout(() => document.querySelector("#consultOpinion")?.scrollIntoView({ behavior: "smooth", block: "start" }), 120);
    });
  });

  app.querySelectorAll("[data-save-case]").forEach(button => {
    button.addEventListener("click", () => {
      state.savedCase = true;
      state.savedCases[state.activeCaseId] = true;
      showToast("已收入小满医馆病案架");
    });
  });
}

function showToast(message) {
  toast.textContent = message;
  toast.classList.add("show");
  clearTimeout(showToast.timer);
  showToast.timer = setTimeout(() => toast.classList.remove("show"), 1800);
}

function playInfoUnlockEffect(cue) {
  if (!cue?.label) return;
  const target = app.querySelector("[data-toggle-info-panel]");
  if (!target) return;
  const pairs = app.querySelectorAll(".chat-pair");
  const lastPair = pairs[pairs.length - 1];
  const source = lastPair?.querySelector(".coach-tip") || lastPair?.querySelector(".bubble.light") || app.querySelector("#freeInquiryInput");
  if (!source) return;
  const startRect = source.getBoundingClientRect();
  const endRect = target.getBoundingClientRect();
  const chip = document.createElement("div");
  chip.className = "info-fly-chip";
  chip.textContent = cue.label;
  const startX = startRect.left + Math.min(startRect.width - 24, 40);
  const startY = startRect.top + Math.max(18, Math.min(startRect.height / 2, 42));
  const endX = endRect.left + endRect.width / 2;
  const endY = endRect.top + endRect.height / 2;
  chip.style.left = `${startX}px`;
  chip.style.top = `${startY}px`;
  chip.style.setProperty("--fly-x", `${endX - startX}px`);
  chip.style.setProperty("--fly-y", `${endY - startY}px`);
  document.body.appendChild(chip);
  requestAnimationFrame(() => {
    chip.classList.add("animate");
    target.classList.add("info-entry-pulse");
  });
  playInfoUnlockTone();
  chip.addEventListener("animationend", () => {
    chip.remove();
    target.classList.remove("info-entry-pulse");
  }, { once: true });
}

function playInfoUnlockTone() {
  try {
    const AudioCtor = window.AudioContext || window.webkitAudioContext;
    if (!AudioCtor) return;
    playInfoUnlockTone.ctx = playInfoUnlockTone.ctx || new AudioCtor();
    const ctx = playInfoUnlockTone.ctx;
    if (ctx.state === "suspended") ctx.resume();
    const now = ctx.currentTime;
    const gain = ctx.createGain();
    gain.connect(ctx.destination);
    gain.gain.setValueAtTime(0.0001, now);
    gain.gain.exponentialRampToValueAtTime(0.08, now + 0.02);
    gain.gain.exponentialRampToValueAtTime(0.0001, now + 0.28);

    const oscA = ctx.createOscillator();
    oscA.type = "sine";
    oscA.frequency.setValueAtTime(740, now);
    oscA.frequency.linearRampToValueAtTime(880, now + 0.16);
    oscA.connect(gain);
    oscA.start(now);
    oscA.stop(now + 0.28);

    const oscB = ctx.createOscillator();
    oscB.type = "triangle";
    oscB.frequency.setValueAtTime(988, now + 0.04);
    oscB.connect(gain);
    oscB.start(now + 0.04);
    oscB.stop(now + 0.18);
  } catch (error) {
    // Ignore audio failures; the visual cue is still enough.
  }
}

function scrollInquiryToBottom() {
  setTimeout(() => {
    const thread = document.querySelector("#inquiryThread");
    if (thread) {
      thread.scrollTo({ top: thread.scrollHeight, behavior: "smooth" });
      return;
    }
    document.querySelector("#inquiryEnd")?.scrollIntoView({ behavior: "smooth", block: "end" });
  }, 60);
}

function showStageToast(route) {
  if (!isColdCase()) return;
  const progress = getEvidenceProgress();
  const messages = {
    diagnosis: `已完成问诊取证，获得 ${progress.unlockedCount} 条关键证据`,
    assessment: "已完成辨证选方，证型方向待带教评估",
    observation: "已完成辨证学习，进入模拟服药",
    review: "已完成模拟服药，进入病案复盘"
  };
  if (messages[route]) showToast(messages[route]);
}

function getSelected(group) {
  const selections = isColdCase() ? state.coldSelections : state.selections;
  return selections[group]?.selected[0] || "";
}

function hasAskedQuestion(question, key = "") {
  const normalized = normalizeQuestion(question);
  return getCaseState().inquiryRecords.some(record => {
    if (key && record.matchedKey === key) return true;
    return normalizeQuestion(record.question) === normalized;
  });
}

function addInquiryRecord(question, result, source) {
  const caseState = getCaseState();
  const unlockedInfo = unlockInfoByKey(result.key);
  const record = {
    id: `record-${state.activeCaseId}-${caseState.nextRecordId++}`,
    question,
    answer: result.answer,
    matchedKey: result.key,
    relevance: result.relevance,
    source,
    speaker: result.speaker || "patient",
    unlockedInfo: unlockedInfo.category || "",
    unlockedFinding: unlockedInfo.isNew ? unlockedInfo.finding : "",
    judgement: unlockedInfo.isNew && unlockedInfo.judge ? JSON.parse(JSON.stringify(unlockedInfo.judge)) : null,
    coachTip: unlockedInfo.isNew && unlockedInfo.judge ? "" : getInquiryCoachTip(result)
  };
  caseState.inquiryRecords.push(record);
  return unlockedInfo;
}

function unlockInfoByKey(key) {
  const meta = getEvidenceMetaByKey(key);
  if (!meta) return { isNew: false, label: "" };
  const caseState = getCaseState();
  const existing = caseState.unlockedEvidence?.[meta.category];
  if (existing) {
    return { ...meta, isNew: false, label: meta.finding || meta.category };
  }
  caseState.unlockedEvidence[meta.category] = meta.finding;
  if (!caseState.unlockedInfo.includes(meta.category)) caseState.unlockedInfo.push(meta.category);
  return { ...meta, isNew: true, label: meta.finding || meta.category };
}

function getInquiryCoachTip(result) {
  if (isColdCase()) {
    if (result.relevance === "low" || result.key === "未匹配") return "这个问题和当前辨证主线关系不大。可以先回到寒热轻重、汗出、鼻涕、痰色、咽喉和舌脉这些更有分量的线索。";
    return DATA.coldAnswer.inquiryTips[result.key] || "";
  }
  if (result.relevance === "low" || result.key === "未匹配") {
    return "这个问题和当前失眠辨证主线关系不强。可以先回到病程、胸胁、月经、心悸、饮食二便这些更关键的方向。";
  }
  return "";
}

function normalizeQuestion(question) {
  return String(question).replace(/[\\s？?。！!，,、：:；;]/g, "");
}

function matchPatientAnswer(question, preferredKey = "") {
  const normalized = question.replace(/\s/g, "");
  if (/舌象|舌苔/.test(normalized) || preferredKey === "舌象") {
    return {
      key: "舌象",
      answer: `${getActivePatient().basics?.舌象 || "舌象信息见基础信息卡。"}`,
      relevance: "medium",
      speaker: "system"
    };
  }
  if (/脉象|浮紧|浮数/.test(normalized) || preferredKey === "脉象") {
    return {
      key: "脉象",
      answer: `${getActivePatient().basics?.脉象 || "脉象信息见基础信息卡。"}`,
      relevance: "medium",
      speaker: "system"
    };
  }
  const knowledgeBase = getActiveKnowledgeBase();
  if (isColdCase()) {
    const priorityKey = /痰色|咳痰|白痰|黄痰|痰/.test(normalized)
      ? "痰"
      : /鼻涕|流涕|清涕|黄涕|清鼻涕/.test(normalized)
        ? "鼻涕"
        : /咽喉|咽痛|喉咙|嗓子|红肿/.test(normalized)
          ? "咽痛"
          : "";
    const priority = knowledgeBase.find(item => item.key === priorityKey);
    if (priority) return priority;
  }
  const preferred = knowledgeBase.find(item => item.key === preferredKey);
  if (preferred) return preferred;

  const matches = knowledgeBase
    .map(item => {
      const alias = item.aliases.find(aliasText => normalized.includes(aliasText));
      return alias ? { item, length: alias.length } : null;
    })
    .filter(Boolean)
    .sort((a, b) => b.length - a.length);

  if (matches.length) return matches[0].item;

  return {
    key: "未匹配",
    answer: isColdCase()
      ? "这个情况我没有特别明显。"
      : (question.length % 2 === 0
        ? "这个情况我没有特别明显。"
        : "这个问题我不太确定，平时没有特别明显的感觉。"),
    relevance: "low"
  };
}

function getInquiryAnalysis() {
  if (isColdCase()) return getColdInquiryAnalysis();
  const records = getCaseState().inquiryRecords;
  const keyIssueMap = {
    病程: "high",
    胸胁胀满: "high",
    月经: "high",
    口苦咽干: "medium",
    心悸健忘: "medium",
    纳差便溏: "medium",
    饮食: "medium",
    大便: "medium",
    潮热盗汗: "medium",
    痰多胸闷: "medium",
    怕冷怕热: "low-medium",
    小便: "low"
  };
  const reviewKeys = ["病程", "胸胁胀满", "月经", "口苦咽干", "心悸健忘", "纳差便溏", "潮热盗汗", "痰多胸闷"];
  const hitKeys = [...new Set(records.map(record => record.matchedKey).filter(key => keyIssueMap[key]))];
  const keyHitCount = hitKeys.filter(key => reviewKeys.includes(key)).length;
  const weakCount = records.filter(record => record.relevance === "low" || record.matchedKey === "未匹配").length;
  let completenessScore = 48;
  if (keyHitCount <= 1) completenessScore = 48 + keyHitCount * 7;
  if (keyHitCount === 2) completenessScore = 68;
  if (keyHitCount === 3) completenessScore = 74;
  if (keyHitCount >= 4) completenessScore = Math.min(90, 78 + (keyHitCount - 4) * 4);
  if (hitKeys.includes("胸胁胀满") && hitKeys.includes("月经")) completenessScore += 4;
  completenessScore = clamp(completenessScore - weakCount * 4, 45, 92);

  const evidenceBonus = (hitKeys.includes("胸胁胀满") ? 5 : 0) + (hitKeys.includes("月经") ? 5 : 0) + Math.min(6, keyHitCount * 2) - weakCount * 3;
  const missingKeys = reviewKeys.filter(key => !hitKeys.includes(key));
  const usefulKeys = hitKeys.filter(key => reviewKeys.includes(key));
  const usefulText = usefulKeys.length ? `你追问了“${usefulKeys.join("、")}”，这些问题能够帮助判断病程、肝郁、化火及妇科相关证据，属于有效追问。` : "你还没有命中关键辨证追问，建议先围绕病程、胸胁、月经、寒热、心悸、饮食二便继续追问。";
  const missingText = missingKeys.length ? `本案还建议追问“${missingKeys.slice(0, 4).join("、")}”，以便排除阴虚火旺、心脾两虚和痰热扰心等可能。` : "本案核心鉴别问题覆盖较完整，后续可继续细化睡眠节律与服药后反应。";
  const weakText = weakCount ? "你追问的部分问题与本案失眠、情志、月经前症状关系较弱。建议优先围绕睡眠特点、情绪、胸胁、寒热、二便和月经展开。" : "本轮追问基本围绕失眠、情志和辨证鉴别展开，问题方向较集中。";
  const nextAdvice = missingKeys.length
    ? `下次建议补充追问${missingKeys.slice(0, 3).join("、")}，让虚拟病人的证据链更完整。`
    : "下次可以继续追问症状变化的时间、诱因和复诊反馈，让模拟问诊更接近完整病案采集。";

  return {
    total: records.length,
    hitKeys,
    keyHitCount,
    weakCount,
    completenessScore,
    evidenceBonus,
    pointComment: usefulText,
    missingComment: missingText,
    weakComment: weakText,
    nextAdvice
  };
}

function getFeedbackScenario() {
  if (isColdCase()) return getColdFeedbackScenario();
  const syndrome = getSelected("主证判断");
  const formula = getSelected("参考方选择");
  if (syndrome === "肝郁气滞" && formula === "逍遥散合酸枣仁汤加减") return DATA.feedbackMap.liver;
  if (syndrome === "心脾两虚" && formula === "归脾汤加减") return DATA.feedbackMap.spleen;
  if (syndrome === "阴虚火旺" && formula === "黄连阿胶汤加减") return DATA.feedbackMap.yinFire;
  if (syndrome === "痰热扰心" && formula === "温胆汤加减") return DATA.feedbackMap.phlegm;
  return DATA.feedbackMap.default;
}

function getColdStudentSelections() {
  const herbSelection = state.coldSelections["中药配方选择"].selected.length
    ? state.coldSelections["中药配方选择"].selected
    : getColdGroupHerbSelection(state.coldGroupSelections || []);
  return {
    syndrome: state.coldSelections["证型判断"].selected[0] || "",
    method: state.coldSelections["治法选择"].selected[0] || "",
    formula: state.coldSelections["方剂选择"].selected[0] || "",
    herbs: herbSelection,
    patent: state.coldSelections["中成药选择"].selected
  };
}

function getColdAssessmentResult() {
  const answer = DATA.coldAnswer;
  const selections = getColdStudentSelections();
  const inquiry = getInquiryAnalysis();
  const herbHits = selections.herbs.filter(item => answer.herbs.includes(item)).length;
  const herbWrong = selections.herbs.filter(item => !answer.herbs.includes(item)).length;
  const coldHerbWrong = selections.herbs.filter(item => answer.coldHerbs.includes(item)).length;
  const patentHits = selections.patent.filter(item => answer.patent.includes(item)).length;
  const patentWrong = selections.patent.filter(item => !answer.patent.includes(item)).length;
  const formulaMeta = answer.formulaGraph[selections.formula] || { score: 50, note: "该方不在本案感冒图谱的主要路径中。", relation: "需调整" };
  const syndromeScore = selections.syndrome === answer.syndrome ? 90 : selections.syndrome === "风热感冒" ? 62 : 58;
  const formulaScore = formulaMeta.score;
  const herbScore = clamp(Math.round((herbHits / answer.herbs.length) * 92) - herbWrong * 5 - coldHerbWrong * 6, 35, 95);
  const patentScore = clamp(62 + patentHits * 11 - patentWrong * 8, 35, 95);
  const safetyScore = clamp(88 - coldHerbWrong * 8 - patentWrong * 4, 45, 92);
  const dimensions = [
    ["问诊完整度", inquiry.completenessScore],
    ["证型判断", syndromeScore],
    ["方证匹配", formulaScore],
    ["中药配方", herbScore],
    ["中成药选择", patentScore],
    ["用药安全性", safetyScore]
  ];
  const score = Math.round(dimensions.reduce((sum, item) => sum + item[1], 0) / dimensions.length);
  const tag = score >= 82 ? "方向较准确" : score >= 68 ? "部分方向正确" : "需要回看证据";
  return {
    score,
    tag,
    dimensions,
    comment: selections.syndrome === "风寒感冒"
      ? `你抓住了“恶寒重、发热轻、无汗、鼻流清涕、痰白清稀、苔薄白、脉浮紧”等关键证据，能够判断为风寒感冒。方剂图谱判断：${formulaMeta.note}`
      : "当前证型判断与本案关键证据仍有偏差。请回看恶寒明显、无汗、清涕、痰白清稀、苔薄白、脉浮紧等风寒表证证据。",
    herbComment: coldHerbWrong
      ? "你选择了较多偏寒凉清热药物，如金银花、连翘、石膏、黄芩等。本案热象不明显，这类药物更偏风热或实热方向，不宜作为主线。"
      : "荆芥、防风、羌活、独活等药物与风寒束表方向匹配。中药配方应围绕辛温解表、宣肺散寒展开。",
    patentComment: patentWrong
      ? "风寒感冒颗粒、荆防颗粒、感冒清热颗粒更符合本案风寒外束方向。银翘解毒丸、桑菊感冒片、双黄连口服液更偏风热或热毒，不是本案首选。"
      : "风寒感冒颗粒、荆防颗粒、感冒清热颗粒更符合本案风寒外束的方向。"
  };
}

function getColdFeedbackScenario() {
  const selections = getColdStudentSelections();
  const answer = DATA.coldAnswer;
  const coldHerbWrong = selections.herbs.filter(item => answer.coldHerbs.includes(item)).length;
  const selectedGroups = state.coldGroupSelections || [];
  const mainGroups = ["wind-cold", "lung-phlegm", "supportive"];
  const hasWindHeatGroup = selectedGroups.includes("wind-heat");
  const hasDampYinGroup = selectedGroups.includes("damp-yin");
  const exactMainGroups = mainGroups.every(group => selectedGroups.includes(group)) && !hasWindHeatGroup && !hasDampYinGroup;
  const mainGroupCount = selectedGroups.filter(group => mainGroups.includes(group)).length;
  if (selections.syndrome === "风寒感冒" && selections.formula === "荆防败毒散加减") {
    if (exactMainGroups) return DATA.coldFeedbackMap.correct;
    if (hasWindHeatGroup || coldHerbWrong >= 2) return DATA.coldFeedbackMap.coldHerbs;
    if (hasDampYinGroup || mainGroupCount < 3) return DATA.coldFeedbackMap.groupMismatch;
  }
  if (selections.formula === "麻黄汤加减") return DATA.coldFeedbackMap.mahuang;
  if (selections.formula === "桂枝汤加减") return DATA.coldFeedbackMap.guizhi;
  if (selections.syndrome === "风热感冒" || ["银翘散加减", "桑菊饮加减"].includes(selections.formula) || selections.patent.includes("银翘解毒丸")) return DATA.coldFeedbackMap.windHeat;
  if (selections.syndrome === "暑湿感冒" || ["新加香薷饮加减", "藿香正气散加减"].includes(selections.formula) || selections.patent.includes("藿香正气水")) return DATA.coldFeedbackMap.dampSummer;
  if (selections.formula === "白虎加人参汤") return DATA.coldFeedbackMap.windHeat;
  return DATA.coldFeedbackMap.windHeat;
}

function getColdObservationPreview() {
  const selections = getColdStudentSelections();
  if (selections.formula === "麻黄汤加减") return "风险观察：同属风寒网络，但发散力量偏强，需要观察鼻塞清涕与体力反应。";
  if (selections.formula === "桂枝汤加减") return "风险观察：桂枝汤偏表虚有汗，本案无汗证据明显，可能改善有限。";
  if (selections.syndrome === "风热感冒" || ["银翘散加减", "桑菊饮加减", "白虎加人参汤"].includes(selections.formula)) return "风险观察：风热或热盛方向可能无法改善怕冷、清涕和身痛。";
  if (selections.syndrome === "暑湿感冒" || ["新加香薷饮加减", "藿香正气散加减"].includes(selections.formula)) return "风险观察：若胃肠症状不突出，化湿方向可能无法切中主证。";
  return "预计观察重点：怕冷、鼻涕、头痛身痛、咳痰变化。";
}

function getAssessmentResult() {
  if (isColdCase()) return getColdAssessmentResult();
  const syndrome = getSelected("主证判断");
  const formula = getSelected("参考方选择");
  let base = DATA.assessmentMap.default;
  if (syndrome === "肝郁气滞" && formula === "逍遥散合酸枣仁汤加减") base = DATA.assessmentMap.liver;
  if (syndrome === "心脾两虚" && formula === "归脾汤加减") base = DATA.assessmentMap.spleen;
  if (syndrome === "阴虚火旺" && formula === "黄连阿胶汤加减") base = DATA.assessmentMap.yinFire;
  if (syndrome === "痰热扰心" && formula === "温胆汤加减") base = DATA.assessmentMap.phlegm;

  const inquiry = getInquiryAnalysis();
  const baseScore = Object.fromEntries(base.dimensions);
  const keyEvidence = clamp((baseScore["关键证据抓取"] || 70) + inquiry.evidenceBonus, 45, 94);
  const dimensions = [
    ["问诊完整度", inquiry.completenessScore],
    ["关键证据抓取", keyEvidence],
    ["辨证方向", baseScore["辨证方向"] || 70],
    ["方证匹配", baseScore["方证匹配"] || 70],
    ["用药安全性", baseScore["用药安全性"] || 75]
  ];
  const score = Math.round(dimensions.reduce((sum, item) => sum + item[1], 0) / dimensions.length);
  return { ...base, score, dimensions };
}

function clamp(value, min, max) {
  return Math.max(min, Math.min(max, value));
}

function escapeHTML(value) {
  return String(value).replace(/[&<>"']/g, char => ({
    "&": "&amp;",
    "<": "&lt;",
    ">": "&gt;",
    '"': "&quot;",
    "'": "&#039;"
  }[char]));
}

render("home");
